# Changelog

All notable, dataset-level changes are recorded here. This is the human-readable companion to the Git history: Git records every line change, this records the decisions that matter to a data *consumer*. Format follows [Keep a Changelog](https://keepachangelog.com/); versions follow [Semantic Versioning](https://semver.org/).

## organizational_forms — 2026-08-29 (iii)

**`partenrederij` and `begijnhof` coded, 64 rows.** 24 → 26 rows over 24 institutions; 485 → 549 cells. Two new type rows. The first two forms coded in a blind working copy since the kō batch, and the coded siblings were genuinely not visible during coding. Sources: De ruysscher 2023 for the ship form; Moran 2018, Overlaet 2015 and Voigt 2012 for the beguinage.

- **`LP3=1` fills a value that had no instance** anywhere in this dataset — the first form coded as having standing to sue as an entity. Court beguinages litigate as bodies against the guilds across a century and win. Zero-instance values 11 → 10.
- **Three dependence mappings stop being functions, all on substantive values.** `LP2 → LP3` (begijnhof): holding property as an entity does not determine standing to sue as one. `CF2 → AP3` and `CF2 → LR1` (partenrederij): an agent insulated from his principal's capital is not thereby an agent whose own estate lies open. Both results are contingent on cells flagged for review — see logbook 4.
- **The risk-pooling facet stops being a near-constant.** `LR4` stood at 14 `0` against 2 `1`; the batch adds one to each side, from unrelated sources on a single criterion — does the form mutualise a *member-specific* hazard, or divide one common exposure? `LR5` gains its second `diversifying`.
- **`FP1=mutual-provision` is still empty, which is the batch's negative result.** Both forms were selected to fill it and neither did. The census still contains no form whose declared purpose is mutual provision, and the two candidates the brief identified are now spent.
- **Coverage in this matrix is currently a property of the bibliography.** `partenrederij` is 7/8 on WP1 and 1/13 on WP2; `begijnhof` is the reverse. Neither profile reflects its institution — De ruysscher wrote on liability doctrine, Moran on administration. `entity-shielding` is 0/3 on both and remains the emptiest declared WP2 facet.
- **Considered and rejected:** splitting `begijnhof` at 1585 (falsification condition recorded on the type row); a `mixed` value for `LR1`; a repair to `MG4`, which misfits on both forms; a separate type for English ship part-ownership, which is recommended but was not asked for. Logbook 4.
- **No `version` bump.** `organizational_forms` stays at `0.8.0`. The additions are rows and vocabulary rows; CONTRIBUTING §5 ties the bump to data-package schema growth, of which there has been none — the same reasoning as 2026-08-29 (ii) and 2026-08-19. `codebook.md` is therefore unchanged and was not regenerated.
- **All 64 rows are `coder=ai`, `reviewed_by=none`, `review_status=unreviewed`.** Four cells are flagged for adjudication before anything is built on them: `partenrederij LR4=0` and `CF1=P`, `begijnhof LR4=1` and `LP3=1`.

## organizational_forms — 2026-08-29 (ii)

**`maona_chios` coded, 32 rows.** 23 → 24 rows over 22 institutions; 453 → 485 cells. New type row; `maona_corsica.cooccurs_with` amended. Sources: Taviani 2022 (Table 1.1, the constitutive texts of the Maona Vecchia and Nuova) and Balletto 1990 (Donato di Chiavari's Chios register, 1393–94), which is independent of Taviani.

- **`TS3=1` fills a value that had no instance** anywhere in this dataset — the first form coded as revocable. Decided, not defaulted; the losing reading is in the cell. Zero-instance values 12 → 11.
- **`LP2` revised 0 → P on acquisition**, after Balletto showed the maona contracting, farming out revenues and taking a legacy in its own name.
- **A result reported earlier the same day was falsified by this row** — the `CI4` gradient across `maona_corsica`/`casa_san_giorgio`/`voc`. Retained in weaker form as a plateau-and-threshold claim. Logbook 4, 2026-08-29 (ii).

### Not done here
- **No `version` bump.** Rows and vocabulary rows only; CONTRIBUTING §5 ties the bump to schema growth.
- **The 1347 Old Maona not coded.** A separate constitution differing on `TS2`, `MG3` and `CI3`; a third maona row is a diversity-budget question, not a coding one.
- **`CI3` and `CI4` remain single-sourced.** Balletto's register records no share transfers, so the two cells carrying the comparison between the maone still rest on Taviani's table alone.


## organizational_forms — 2026-08-29

**Five forms coded, 160 rows, 18 → 23 rows over 21 institutions.**

- `casa_san_giorgio` (1407–1805) from Taviani 2022, Felloni 2010, Miner 2020, Rohan 2025.
- `maona_corsica` (1378–1407) from Petti Balbi 1981, legible only after OCR this session.
- `voc` **replaced** by three dated rows — `voc_1602`, `voc_1612`, `voc_1623` — on the rule that a type spanning both values of a characteristic must be split. All three carry `cooccurrence_basis=same-institution`: the VOC contributes one case to any *n*.

**Considered and rejected.**

- **Revising the WP2 component set** after Taviani 2022, 15 argued that its constituent bundle is the *Historische Rechtsschule*'s anachronism when applied to San Giorgio. Rejected on `CHARACTER-CODING.md` rule 2: the set is declared before the coding it will be used on, and revising it after seeing the result it produced is retrospective selection. Declared a comparative concept instead — logbook 1.
- **A characteristic for the volatility or liquidity of the underlying venture.** Dari-Mattiacci et al. explain the failure of the Toulouse model to generalise by the mills' immovable assets and stable annual income, and no characteristic records that. Rejected per "add forms, not features"; recorded as an instrument limitation.
- **Repairing `MG3`**, which cannot record an open membership with a property- and sex-qualified franchise (San Giorgio), and **`MG2`**, which cannot record who constitutes a collegial body — San Giorgio's investors elected their protettori, the VOC's directors were appointed by the provincial estates, and both read `collective`. Flagged in the cells; a repair belongs in a later commit, not the batch that motivated it.
- **`maona` as one row.** Corsica and Chios separate on `CI3`; `maona_chios` is separately codeable and was not coded.
- **`LP3=1` and `LR1=limited` for San Giorgio**, both predicted by the acquisition brief and both refused by the sources. Left `.NR`.

### Not done here
- **No `version` bump.** `organizational_forms` stays at `0.8.0`. An earlier draft of this entry bumped it to 0.9.0; that was wrong and is reverted. The additions are rows and vocabulary rows, and CONTRIBUTING §5 ties the bump to data-package schema growth, of which there has been none — the reasoning recorded under 2026-08-19, "No `version` bump, and the reason is not oversight."
- **`datasets/loss_mitigation_forms/codebook.md` regenerated separately** (563 → 740 rows), catching up the kō merge which added the rows without the regeneration step. No coding in that dataset was touched.


## [Unreleased] — 2026-08-20

### Added
- `datasets/loss_mitigation_forms/data.csv`: **131 rows**, `LM-0610`–`LM-0740`, coding the seven parked kō/tanomoshi forms — `ko_horyuji_50kan` (20 rows), `ko_furuichi_sengan` (21), `ko_meio_1498` (20), `ko_gotensama_1843` (16), `ko_choko_1854` (12), `ko_kosaburo_1802` (21), `ko_mitarai_hanei_1840` (21). **62 cells are `.NR` and 11 `.NA` — 56 per cent of the batch is missing**, which is the intended shape and not a coverage failure. `RB1`, `RB2` and `RB4` are absent from all seven, as from every membership-fund row in the census.
- `vocabularies/loss_mitigation_type.csv`: `boundary_basis` and `boundary_confidence` confirmed on all seven type rows touched (`documented-instance` / `high` throughout); `key_source` rewritten on each to record the coding and its corrections.
- Two values new to the Japanese block. **`ko_furuichi_sengan DR1=open-ended`**, the first kō in the census that is not a completed cycle (三浦 1959, 790: かなり恒久的な性格をもち他に融資していた). **`ko_meio_1498 GV1=collective`**, the first positive governance value on any kō row — before this, `GV1` was `.NR` on every kō but `ko_daikokuya_1848`. `PY2=auction` doubles from one occurrence to two, gaining a pre-Meiji instance in `ko_mitarai_hanei_1840`.

### Corrected
- **`ko_meio_1498` carries no 徳政 clause, and the row's `key_source` said it did.** The clause 一、縦雖有天下一同徳政、此懸支懸銭不可有改動之儀事 is at 三浦 1959, **790**, and belongs to `ko_horyuji_50kan` alone; the 明応七年 掟書 is quoted in full at 781 and says nothing about 徳政. The article's only other 明応 occurrence, at 792, is a footnote on 鋳物師. The two rows had been paired on a clause one of them does not have.
- **`ko_horyuji_50kan`: 極楽頼母子 and 経蔵修理用途 both belonged elsewhere.** 極楽 is the 延慶三年 (1310) 法隆寺 惣社明神造替 kō (三浦 780–781, after 林屋辰三郎), a different association; 経蔵修理用途 is `ko_furuichi_sengan`'s. The 五十貫 kō's own declared purpose is the 寺用の法服楽器. Its winner's levy is 置銭五貫文, decided from the page image against the PDF's own OCR layer, which renders it 澱三五貫文.
- **`ko_kosaburo_1802`: the せり belongs to a different association.** 松永 1999's remark that せりが行われた on the interest portion runs from 284 to the head of 285 and concerns the 文化四年赤沢村（吉川町）茂右衛門宿頼母子, a 一両掛 / 二一人前 kō of 1807; the 小三郎 discussion begins at the next sentence. It is doubly hedged (らしく … かも知れない) even there. **`PY2` is `.NR`, not auction** — coding from the annotation would have made this the census's second bidding kō on a passage about a third association.
- **`ko_choko_1854 cooccurrence_basis` raised `same-community` → `same-persons`**, and `ko_gotensama_1843` now lists `ko_choko_1854` as well. 小林 1986, 308: 同一人物が前後三つの頼母子講に名をつらねて; at 300, the 大黒屋 銀集帳's contents and membership are 先行の「御殿様」頼母子講と殆ど同一. No similarity or configurational claim may count the 長興寺村 trio as three independent draws.

### Why — the two collapse questions, and neither collapses
`ko_gotensama_1843` / `ko_daikokuya_1848` / `ko_choko_1854` are **three associations**, on 小林's own evidence: he numbers them 第一次/第二次/第三次 and titles §5 三つの頼母子講; they **overlapped** (二重興行, 1848–52, so they cannot be phases of one thing); the 主宰者 changed; and the flat 家掛り on the landless runs 2匁8分 → 8分5厘 → 1匁2分 across them. `ko_kosaburo_1802` and `ko_hokinosawa_1820` are likewise two associations — different district, village, year and document — and 松永 contrasts the regions himself at 284 (頸城郡農村には引取り型はあまりなく、返金には利子を付けて返した). Nothing collapsed, so no row was padded.

### Known limits a consumer should read before using these rows
- **The batch was not blind.** The brief specified a blind working copy at `~/blind-ko-2026-08-20`, which does not exist; the live checkout was used instead, so the coded sibling rows were visible throughout. The discipline against sibling contamination was applied by hand, passage by passage, not by construction.
- **The census cannot tell these forms apart.** With all seven added, only **four pairs** in the nineteen-form Japanese block separate on a substantive value, and **none of the four sibling pairs separates on anything** — every difference is one source filling a cell the other leaves empty. `ko_horyuji_50kan` separates from nothing in the census. All nineteen kō signatures are nominally distinct, but distinct by *pattern of missingness*, which is a fact about surviving archives rather than about institutions. See logbook 4 (2026-08-20).
- **`ko_choko_1854` is the emptiest row in the census** — twelve cells, ten `.NR`. Its `PR1` is `.NR` and deliberately not `0`: the two siblings take `0` on their own 銀集帳 evidence, and for the third 小林 has no ledger at all, so an absence asserted there would be an absence in the archive.

### Flagged for review, not changed
- **`ko_furuichi_sengan MC1=pooling` at `low`.** The same 三浦 sentence that gives `DR1=open-ended` says the fund 他に融資していた — lent beyond its members — which strains `MC1`'s "no outsider". A reviewer may reasonably move it to `.NR`, joining the seven other forms whose sort key cannot be set.
- **`ko_kosaburo_1802 LS2=1` at `medium`.** 松永: 集まった利息はまだ寄金を引取らない講員が分配する, on a published per-head schedule. That is a claim on the fund's *income*, not a residual at winding up, and `LS2` says "surplus". Read strictly it is `.NR`, and the row then loses its only difference from `ko_hokinosawa_1820`.
- **`ko_daikokuya_1848 MB3=compulsory` at `high` was not touched and is in tension with its own source.** `LM-0216` rests on 小林 at 298 (強制的に集めた); 小林 at 308 argues the other way — 三度にわたる講に…それぞれの講が理不尽な強制であれば拒否できたはずであるが、それをせず — and concludes 農民の積極性はみとめられる. The value may stand; the `high` probably should not.

### What was considered and rejected
No characteristic added, split or repaired, though the batch produced **eight** strains: `MB3` fails on three rows *in opposite directions* (a levy participants could decline and did not, against a membership formally elective and materially inescapable); `CN2` gains a second independent two-part tariff and a third, distinct problem — a per-share against per-head locator in `ko_mitarai_hanei_1840`; `RB3` and `LS3` on `ko_meio_1498` fail on locator rather than on values; `GV1` cannot name either a general meeting with a standing officer beside it or an association that amends its own terms on member demand; and **no characteristic in the census asks about a hazard to the fund itself**, which is what `ko_horyuji_50kan`'s 徳政 clause defends against. Each strain is recorded as `.NR` for no-fitting-value in the row where its evidence lives. Proposing a repair in the same batch as the coding that motivates it is retrospective selection and breaks the falsifiability commitment in `CHARACTER-CODING.md`. `views/` not regenerated; it is stale independently of this batch. `datapackage.json` untouched — this is data growth, not schema growth.

### Checks
`frictionless validate`: **VALID**. `check_vocabularies.py`: 2 files, 82 codes, all values within `allowed_values`, enums agree. `check_dependence.py`: 0 problems (applicability, articulation and redundancy-group). Hand sweeps of `value`-against-`allowed_values`, every enum field, `.NA` propagation through `confidence`/`articulation`/`source_ref`/`source_lang`/`source_read`, `articulated` never on `[verify]`, and `record_id` uniqueness against the existing 609: clean.

Reasoning in logbook 2, logbook 4 and logbook 5 (2026-08-20); full proposal and adjudications in `proposed/NOTES-ko-2026-08-20.md`. Every row carries `coder=ai` and `review_status=unreviewed`.

## [Unreleased] — 2026-08-19

### Added
- `datasets/loss_mitigation_forms/data.csv`: **46 rows**, `LM-0564`–`LM-0609`, coding two forms on 23 characteristics each. `avariz_fund` (`LM-0564`–`LM-0586`) and a new `confraternity_fund_it` (`LM-0587`–`LM-0609`). 22 of the 46 cells are `.NR`; `RB1`, `RB2` and `RB4` are absent from both rows, as from every membership-fund row in the census.
- `vocabularies/loss_mitigation_type.csv`: **`confraternity_fund_it`**, "Confraternity and guild mutual support — Italian", 11c–15c. The first child of the regional split that `confraternity_fund`'s `key_source` has required since 2026-08-13; the parent becomes an uncoded umbrella on the `guild_mutual_aid` model.
- `boundary_basis` and `boundary_confidence` filled on all seven type rows touched.

### Changed
- **`avariz_fund` renamed and rescoped**, "Avarız-hane fund (Ottoman communal levy)" → "Mahalle / köy avarız sandığı (quarter chest for the communal levy)". *The old title was an error of fact.* An *avârız hâne* is the fiscal accounting unit by which a state war tax was apportioned — three to ten real households counted as one — not a fund, and the tax's payers get no pool. The institution that exists is the quarter chest into which successive endowments were gathered. This is the split candidate `avariz_vakfi_kirkcesme`'s `key_source` has carried as "NOT YET EVIDENCED"; Kars 2020 and Gürsoy 2019 supply it. It separates from Kırkçeşme on substantive values, not via `.NA`: `MC1` pooling/allocation, `GV1` collective/`.NR`, `MB3` voluntary/`.NA`.
- `huiguan`, `gongsi`, `huf`, `caravan_partnership`: `key_source` rewritten to record **why each is not coded**, so that an empty row no longer reads as work pending rather than a determination.

### Why
Consumers should know that four of the six forms named in this batch are absent by decision. `huiguan` and `gongsi` have no source in the collection treating them as loss mitigation at all — the one relief fact belongs to a *jiao*, and is a collective good for a town. `huf` rests on an 1855 colonial revenue glossary whose "coparcenary" entries concern land tenure, and is recommended for removal to `organizational_forms`. `caravan_partnership` is blocked on readability, not judgement: its four sources are an image-only scan, two HTML stubs and a two-page fragment. See logbook 2 (2026-08-19).

### Corrected within the batch
- **`avariz_fund MC1` corrected `pooling` → `allocation` the same day it was added**, after a pass over collection `B9Z8XKHJ`. Çizarça 1995's Bursa registers show an avarız fund's capital growing from retained surplus and transfers from *other waqfs* rather than from residents, founders endowing for social standing, and beneficiaries who are "the residents of a certain district" — donor and beneficiary disjoint, which is this census's test for `allocation`. `GV1` further reduced to `low` on trustee self-dealing evidenced in two cities. **Consequence: the pooling split-candidate `avariz_vakfi_kirkcesme` has carried as "NOT YET EVIDENCED" is still not evidenced, and this row is not it.** The row now rests on aggregation and governance rather than mechanism, and a reviewer may reasonably merge the two rows. See logbook 4 (2026-08-19 (iii)).
- **Kıvrım 2019 acquired mid-session; `MC1` raised to `high`, `HZ1` reduced to `medium`, `GV1` raised to `medium`.** The second-province source shows that Kars 2020 reproduces its sentences verbatim and cites it, so two cells justified on "two sources agreeing" were resting on one claim and its copy. `MC1=allocation` is strengthened because Kıvrım's own evidence is nine named individual donors relieving "the people of their own quarter"; `HZ1`'s justification was withdrawn; `GV1` rose on Ayntab court cases that are genuinely his own. See logbook 4 (2026-08-19 (iv)).
- **`cash_waqf` reopened and left parked.** Çizarça's expenditure classification is the parking reason quantified; Mandaville 1979 adds only the validity controversy already recorded.

### Not done here
- **No characteristic added or split**, though the batch produced three candidates: `PY0` failed on a second Ottoman form, `PY1` lacks the `mixed` that `VF2` has, and nothing in the census can hold the residual liability a pool exists to absorb. Proposing a repair in the same batch as the coding that motivates it is retrospective selection. Recorded in logbook 4 (2026-08-19 (ii)).
- **`huf` not removed**, only recommended. **`avariz_vakfi_kirkcesme` `VF1` and `PY0` not changed**, only flagged for review.
- **`avariz_fund` rests on one of two relevant Zotero collections.** Coded against `CGNZ38XG` as briefed; the topical collection `B9Z8XKHJ` (28 items) was not consulted. Three of its items have since been read and moved `RB3` to `low` and `GV1` to `medium`; 8 readable items remain unread and 9 are unavailable. Every cell on that row is provisional. See logbook 5 (2026-08-19 (ii)).
- **`views/` not regenerated.** It is stale independently of this batch — 33 forms committed where the pre-existing `data.csv` already gives 38.
- No schema change, so `datapackage.json` `version` is unbumped and `codebook.md` unregenerated.

## [Unreleased] — 2026-08-18 (ii)

### Added
- `vocabularies/loss_mitigation_characteristic.csv` and `vocabularies/organizational_form_characteristic.csv`: a **`locator`** column, stating what each characteristic's role values are predicated *of*. `RB1` and `RB2` both take `arrangement`. Rationale in logbook 1 (2026-08-18 (ii)).
- `vocabularies/organizational_form_type.csv`: **`societas_maris`**, a new code in this vocabulary — it already existed in `loss_mitigation_type.csv`. The bilateral commenda; Venetian *collegantia*.
- `datasets/organizational_forms/data.csv`: **22 rows.** `OF-0283`–`OF-0293` code `societas_maris` on the eleven characteristics `commenda` carries. The other eleven add `AP3` and `LR1` to `isqa`, `qirad`, `commenda`, `nakai_fictive_household`, `ortoq_equity` and `ortoq_loan`, closing the gap logbook 1 (2026-08-18) opened.

### Changed
- `mudaraba` in `organizational_form_type.csv` is now marked **not coded, as a determination rather than an omission** — the same disposition as `ortoq_alloc`. *Mudārabah* is the Ḥanafī name for the institution this dataset codes as `qirad`; coding both would enter one institution twice under two madhhab labels. Ramli 2018, 97 on al-Sarakhsī.
- `.gitignore` += `proposed*/`, `_transfer*.tar`.

### Why
Splitting the two datasets is vindicated by a pair they disagree about. Unilateral and bilateral *commenda* collapse on the payout — Pryor, Luzzatto and van Doosselaere all call them "essentially the same agreement", each reaching that on the profit side where the economics are identical by construction — and separate on the loss, where the unilateral traveller bears nothing and the bilateral bears a third. `organizational_forms` merges them; `loss_mitigation_forms` separates them. See logbook 4 (2026-08-18 (ii)).

### Not done here
- **`scripts/check_dependence.py` is unchanged**, so the under-reporting logbook 1 (2026-08-18) identifies is still live. Recorded, not fixed.

## [Unreleased] — 2026-08-18

### Added
- **Four forms coded**, `LM-0527`–`LM-0563` — 37 cells over nine characteristics: `commenda_alloc`, `qirad_alloc`, `isqa_alloc`, `societas_maris`. Blind coding from the brief at `claude/allocation-anchors-acquisition-brief.md`; `ortoq_alloc` left uncoded per its own recorded determination. Four replacement rows in `vocabularies/loss_mitigation_type.csv` carrying `boundary_basis` and `boundary_confidence` for each.

### Why, and the three results a consumer should know about
- **`RB4` has variance for the first time** (`isqa_alloc = 0` against `1` everywhere else). **The retirement proposed on `RB4`'s `exemplar` (2026-08-16) and restated in `LM-0526`'s note is withdrawn.** One added form settled what argument could not — the *asiento* pattern again.
- **`PR1 = 0` on all four.** Within `MC1 = allocation` the field now reads 0 / P / 1, so the 2026-08-07 tontine finding — pricing is orthogonal to mechanism, not a stage of it — reproduces *inside* a single mechanism value, from an unrelated literature. Genoa is the strongest case: 4,860 ties, dozens of destinations, only a handful departing from the customary split.
- **`MC1 = allocation` now covers eight forms** and spans the whole `RB1`/`RB2` contrast without being able to express it; the two are independent across the coded set in both directions.

### Considered and rejected
- **Splitting the Ragusan *collegantia* from `societas_maris`**, as logbook 2 (2026-08-16) queued. **Misfiled:** on Held's own conclusion the Ragusan contract is a local variant of the *unilateral* commenda, so the split would be from `commenda_alloc`. Not made.
- **Treating `commenda_alloc` and `qirad_alloc` as two cases.** They return identical values in every coded cell; both type rows carry `contemporary-terminology` at `high` and a warning against counting them twice in any diversity-budget argument.
- **Coding `LS1`, `LS2` and `LS3` for these four.** Rule applied: a characteristic already asked of the same form in `organizational_forms` is not asked again. What these rows contribute is the `RB1`/`RB2` peril/market split, which `organizational_forms` has no cell for.

### Not done here
- **No `version` bump, and the reason is not oversight.** `datasets/*/datapackage.json` is untouched by everything above: the additions are rows, vocabulary rows and a vocabulary *column*, and `vocabularies/*.csv` are not declared frictionless resources. CONTRIBUTING §5 ties the bump to data-package schema growth, and there has been none. `loss_mitigation_forms` stays at `0.6.0` and `organizational_forms` at `0.8.0`; that gap between them is pre-existing and unrelated.
- Both `codebook.md` files were regenerated (526 → 563 and 271 → 293 rows) so CI's drift check passes.
- **This entry was written on 2026-08-19 by a sighted session**, not by the coding sessions themselves. They could not write it: the `[Unreleased]` blocks for 08-16 and 08-17 were withheld from them under the blinding protocol, so neither could see what version was already staged or what had already been recorded. That is a cost of the protocol and is taken up in logbook 4.

## [Unreleased] — 2026-08-17 (iv)

### Changed
- `LM-0525` (`bottomry.LS3`) and `LM-0523` (`bodemerij_amsterdam.RB4`): notes extended on Rossi 2018, 91, acquired 2026-08-18. No values change.

### Why
Rossi supplies the contemporary vocabulary for the settlement patterns behind both cells: **average** — compensation for the actual damage — against **abandonment**, in which the party transfers ownership of the thing-at-risk in return for the full sum, avoiding "the lengthy process of liquidation of damages". Abandonment is the shape of Schuster's *Abtretung des Pfandes*, the Bodmerei's default release, which corroborates `LM-0525`.

**And it renames a fact already coded.** The six Amsterdam acts whose lender takes *pro rato van 't suver gesalveerde* (nos 3231, 3232, 3234, 3235, 3240, 3242) are **average**, not abandonment — the opposite of the Bodmerei default. Taken with `LS3=1`, that is one pattern in two independent clauses: **Amsterdam drafting converts an in-rem, all-or-nothing settlement into a personal, proportional one.** The "dispositive default contracted out of" reading recorded in (ii) is therefore supported by a second clause type, not by the recourse clauses alone.

Rossi also explains why abandonment is nearly invisible in the statutes — only Burgos 1538 and 1572 and the Antwerp *Compilatae* of 1608 provide for it expressly, while Florence, Genoa, Seville, Bilbao, the earlier Antwerp compilations and the by-laws of **Amsterdam 1598** and Rotterdam 1604 are silent, the evidence sitting in notarial declarations. That is a caution for any future reading of this corpus: it is notarial, but it is bodemerijen and protests rather than insurance policies, so the absence of abandonment declarations in it is not evidence either way.

### Considered and rejected
- **A characteristic for the settlement pattern** (average against abandonment). It is a well-formed distinction and the answer is still no: it is a settlement pattern, the census adds forms rather than features, and `RB4` plus the row notes hold what is observed.

### Not done here
- No `version` bump, no value changes, no codebook row-count change.
- The volume is Pihlajamäki, Cordes, Dauchy & De ruysscher (eds.), *Understanding the Sources of Early Modern and Modern Commercial Law* (Brill, Legal History Library 25), **not** the title given in conversation on 2026-08-17, which was inferred from the DOI and wrong. Four further chapters in it bear on this project and are unread: Wubs-Mrozewicz on Danzig 1460–1580, Van Hofstraeten on notarised partnership agreements in the Low Countries, De ruysscher on the normativity of merchant guidebooks, and Sirks on the sources of commercial law in the Dutch Republic — the last two bearing on how much weight Verwer and Wagenaar can carry in the bodemerij coding.

## [Unreleased] — 2026-08-17 (iii)

### Added
- `LM-0526` — `sea_loan.LS3 = 1` at `high`, `articulated`. Schuster 2005, 112 and 120: the ancient sea-loan debtor *haftete stets unbeschränkt mit seinem gesamten Vermögen*, and *eine Beschränkung auf die verpfändeten Gegenstände wie bei der Bodmerei gab es nicht*. The pledge is security only — *konstitutiven Charakter hatte es nicht*. Operative mechanism: on a short sale the lender executed against the whole estate for the difference, and an *Exekutivklausel* in the deed let him do so without first obtaining judgment.

### Changed
- `LM-0525` (`bottomry.LS3`): note updated — the pair is no longer untestable.

### Why
**The differentia now separates, and separating it required rejecting the blind appendix's value.** The appendix proposed `P` for the sea loan on phase-dependence — shielded *intra praestitutos dies* and on total loss, exposed once the ship arrives safely. That reading was marked `analyst-imposed` by its own coder, and it double-counts: **the risk window is `RB4`'s question**, and `RB4` is `1` for `sea_loan`, `bottomry`, `respondentia` and `bodemerij_amsterdam` alike. Folding it into `LS3` also destroys the boundary it was relocated to carry — bottomry has the identical window, so on the phase reading bottomry would be `P` too and the pair could not separate. `LS3` records the **scope** of liability given that the obligation has crystallised, and the appendix's own note concedes the answer: *the general estate is reachable*.

**The result is a discriminant-validity finding in Campbell & Fiske's sense.** `LS3` now reads `1 / 0 / 1` across `sea_loan`, `bottomry` and `bodemerij_amsterdam` where `RB4` reads `1 / 1 / 1`. The characteristic `RB3` was relieved of in favour of does the work; `RB4` still has one observed value across four maritime forms, and the retirement proposed on its `exemplar` 2026-08-16 is correspondingly stronger.

### Not done here
- `respondentia.LS3` left uncoded: it would restate `bottomry`, and logbook 2 forbids counting respondentia as an independent form in any diversity-budget argument.
- **Scope limit on `LM-0526`:** the Athenian evidence was read directly; the Roman side rests on Schuster's general contrast with *dem antiken Seedarlehen* rather than on Roman sources consulted for this cell. Pontoriero 2011 and Benincasa 2011, cited by the appendix, were not re-read.
- No `version` bump: no schema change.

## [Unreleased] — 2026-08-17 (ii)

### Added
- `LM-0525` — `bottomry.LS3 = 0` at `medium`, `articulated`. The differentia relocated from `RB3` to `LS3` on 2026-08-16 now carries a coded value. Schuster 2005, 211–212: the *eigentliche Besonderheit* of the Bodmerei is *eine auf das Pfand beschränkte, dingliche Haftung* — *reine Sachhaftung*, the creditor cannot reach the owner's estate.

### Changed
- `LM-0512` (`bodemerij_amsterdam.MC1`): confidence `high` → `medium`, with the objection registered in the note. `allocation` and `spreading` do not exclude one another and this form satisfies both; coding it `high` earlier the same day was an error, and an independent instance of the non-partition already queried on `MC1`'s `exemplar`.
- `bodemerij_amsterdam.boundary_basis`: `documented-instance` → `structural-difference`, confidence `high`. The two rows now separate on a substantive value of `LS3` — `0` against `1` — rather than on the provenance of the coding, which is what `documented-instance` was recording while the `bottomry` codings were withheld from the blind pass.

### Why
**The shield is a default rule, not a mandatory one, and that reconciles the normative and the transactional evidence instead of setting them against each other.** Schuster's n.17 gives the debtor the right to discharge himself by abandoning the pledge — *sich durch Abtretung des Pfandes von jeder weiteren Haftung zu befreien*, still in the ALR II 8 §2427 — but only *wenn nicht besondere vertragliche Vereinbarungen hinzutraten*. The Amsterdam notaries of 1601–21 add exactly such agreements as routine drafting: an express charge on person and whole estate in eight of 36 acts, sureties renouncing *beneficium excussionis et ordinis* in one, and creditors executing against persons and goods in the protests. The Marseille statutes of 1253/55 already have personal liability arising only by special agreement. So `bodemerij_amsterdam` is not a counter-example to Schuster; it is the escape hatch Schuster names, exercised as a matter of course in one port.

### Not done here
- **`sea_loan.LS3` and `respondentia.LS3` remain uncoded, and this is the live gap.** The original differentia claim is bottomry *against the sea loan*, and that pair is still untested: one member of it now has a value and the other does not. The blind appendix proposes `P` for the sea loan — shielded *intra praestitutos dies*, exposed once the ship arrives safely — but appendix rows are test output, not codings, and must be re-derived before entry.
- No `version` bump: no schema change.

## [Unreleased] — 2026-08-17

### Added
- `bodemerij_amsterdam` — Bodemerij (Amsterdam notarial practice), Dutch / Amsterdam, 1601–1621. `boundary_basis = documented-instance` at `high`. Coded blind from Winkelman 1983 (RGP Grote Serie 184/185/186): 46 of 48 notarial acts, 36 *contracten van bodemerij* and 10 *protesten*.
- `datasets/loss_mitigation_forms/data.csv`: `LM-0512`–`LM-0524`, thirteen cells — `MC1`, `HZ1`, `HZ2`, `LS3`, `VF1`, `VF2`, `DR1`, `DR2`, `RB1`, `RB2`, `RB3`, `RB4`, `PR1`. **The first `articulated` `PR1` in the maritime cohort, and the first substantive `LS3` in it.**

### Why
Second run of the re-coding test, blinded properly this time on the `definition`/`exemplar` split made 2026-08-16. Against the predictions logbook 2 recorded for this form: `PR1=1` **articulated** is *bought*; `LS3=0` is *falsified* — the value is `1`, on express personal and general-estate charges, sureties renouncing *beneficium excussionis et ordinis*, and creditors executing against persons and goods in the protests; "a jurisdiction outside effective canon-law reach" is *complicated* — Verwer has the lender reinsuring to stay inside the maximum-interest law. Three findings of the 2026-08-16 Mediterranean pass were reproduced independently from a Dutch transactional corpus: the fault/deviation class that no cell holds, ship-versus-goods failing to separate the institution, and `RB4`'s window-dependence.

### Considered and rejected
- **Splitting the corpus.** Four axes of internal variation were tested as candidate second forms, in particular the six acts whose peril clause imports the custom of insurance and shares partial loss *penninck ponts gelijck*. Rejected: the other contracts are **silent** on partial loss, not contrary, so the split would code `.NR` as a value. Those six also cluster on three counterparties inside eighteen months.
- **Naming it Hanseatic `bodmerei`**, as logbook 2 proposed. Rejected on the evidence actually read: the corpus is two Amsterdam notarial offices over twenty years, and Frankot's pluralism argument forbids inferring practice from shared compilations in *either* direction.
- **A `CN1` row.** No allowed value holds a premium contingent on success — the opgeld falls due after safe arrival and is not payable at all on loss. `ex-post-assessment` is glossed as the general-average pattern and would have told a consumer the opposite of the truth.
- **Rows for the pool-shaped characteristics** (`MB1`–`MB3`, `CN1`–`CN3`, `PY0`–`PY3`, `LS1`–`LS2`, `GV1`), following `marine_insurance`'s precedent of coding the maritime subset only.

### Not done here
- No schema change, so no `version` bump. `codebook.md` regenerated for row counts only (511 → 524); CI fails on codebook drift.
- `views/loss_mitigation_forms--risk-pooling.md` is **stale on `main` independently of this change** — `build_views.py` reproduces the same 51/81-line diff from a pristine checkout. Left untouched rather than bundled in. `bodemerij_amsterdam` does not belong in that view regardless: it filters `MC1 = pooling` and this form is `spreading`.
- `bottomry.LS3` remains uncoded. It is the declared differentia since 2026-08-16 and `bodemerij_amsterdam` now gives it its first variance; coding it is the obvious next step.

## [Unreleased] — 2026-08-16

### Added
- `vocabularies/loss_mitigation_type.csv`: `boundary_basis` and `boundary_confidence` — on what grounds each row is a distinct form, and how sure. Values: contemporary-terminology | contemporary-legal-form | structural-difference | analyst-split | documented-instance. Filled for 28 of 54 rows from the table's own notes; 26 left `.NR` rather than back-filled.
- `vocabularies/loss_mitigation_characteristic.csv`: `exemplar` column. `definition` now states what a characteristic asks; `exemplar` states what particular forms answered and why it was added.
- `scripts/check_vocabularies.py`: `check_boundary()` — enforces the basis/confidence pairing and both value sets. Fatal, as with the other vocabulary checks.
- `RB3.allowed_values` += `freight`, `general-estate`.
- `source_lang` enum += `pl`, `pt` in `clearing_records`, `loss_mitigation_forms`, `organizational_forms`.
- `logbook/4-appendix-blind-recoding-2026-08-16.csv` — the 78 blind rows, as test output. **Not codings; not for `data.csv`.**
- `.github/workflows/validate.yml`: a `Check dependence columns` step running `scripts/check_dependence.py` over every dataset. `CLAUDE.md` has listed it among the checks before a PR since the script existed, but CI never ran it — so the conditional constraints (`applicability_on`, `dependence_group`) were unenforced on push. frictionless cannot see them, and the vocabulary check covers `value` against `allowed_values` only.

### Changed
- `bottomry.name` → "Bottomry / Bodmerei (sea loan with constitutive pledge and non-recourse liability)"; `respondentia.name` marks "secured on the goods" as English usage.
- `RB3.definition` no longer asserts that it is the cell separating bottomry from respondentia; that gloss moved to `exemplar` with the correction attached.
- `key_source` for the three maritime rows extended with Zeno 1936, Hoover 1926, de Roover 1969, Schuster 2005, Lohsse 2016, Steckley 2001.

### Why
The blind re-coding pre-registered in logbook 4 §2026-08-13 (iv) was run on 2026-08-16. The blinding failed — the value sets stated the codings under test — and the three decisions the leak did not touch (`MC1`, `PY0`, `PR1`) are the three that failed. The `definition`/`exemplar` split is the repair that makes a future test possible.

### Considered and rejected
- A single `boundary` column with values contemporary-terminology | analyst-imposed | disputed. Rejected: `disputed` is a status, not a basis; one column would have asked two questions.
- Reversing the 2026-08-09 `RB3` widening on the ground that `surety` and `personal` were unoccupied. **Withdrawn**: personal suretyship appears in 22 of 91 loan documents in Zeno's Palermo corpus. The original argument read an absence of acquisition as an absence in the world.
- Splitting `RB3` into "is there a specific pledge" and "of what", and splitting `PR1` into "priced ex ante" and "priced from a distribution". Both are well-formed objections and both are deferred: three new characteristics out of one test is what "add forms, not features" exists to prevent. Recorded in logbook 4 and in the vault instead.
- Merging `bottomry` and `respondentia`. Rejected on Schuster: bottomry is a contemporary legal form attested from 1253/55. `respondentia` is recorded as `analyst-split` rather than merged away.

### Not done here
- `PY0.applicability_on = MC1` was tried, tested against the coded data and **rejected**: `avariz_vakfi_kirkcesme` has a fund and real payouts with `MC1=allocation`, so `MC1` is not `PY0`'s gate. Reverted. `scripts/check_dependence.py` keeps only the corrected summary label (`dependence problems`, which sums violations, suspects and articulation problems - the old label said `applicability violations`).
- The datapackage `version` fields are unchanged; bump them with whatever release this lands in.

## [Unreleased]

### Added

- **Three provenance columns added to both censuses, at MS's proposal**, after the van Leeuwen audit showed that nothing in the repository could detect a cell coded from an unread source. `loss_mitigation_forms` **0.5.0 → 0.6.0** (511 rows), `organizational_forms` **0.7.1 → 0.8.0** (282 rows). Both schemas now identical in this respect; all scripts read by field name, so nothing else required changing. **The fact that motivated it: `coder` is `ai` on all 793 rows across both censuses. Nothing in this project has ever been checked by a human.** The `coder` description already said *"'ai' marks a provisional assistant-generated seed coding pending human verification"* — the schema anticipated review and had no field in which to record it.
  - **`source_read`** — `full` | `partial` | `none` | `unknown`, `.NA` where the row has no source. Records whether the **coder** had read `source_ref`. This is the field that would have caught the van Leeuwen failure, and it audits the coder rather than the reviewer. Backfilled `partial` for the eight forms whose sources are known to have been opened in the sessions of 2026-08-12 and 2026-08-13, `unknown` elsewhere — **deliberately conservative: `unknown` means the coding process did not record it, and guessing would repeat the error the column exists to prevent.** Currently 167 `partial`, 289 `unknown`, 55 `.NA` in `loss_mitigation_forms`; **`organizational_forms` is 260 `unknown` and nothing else**, which is a fair statement of where that census stands.
  - **`reviewed_by`** — initials or `none`, `required`. Distinct from `coder`; multiple reviewers separated by `;`. Chosen over a boolean so that it survives the Synergy team without migration.
  - **`review_status`** — `unreviewed` (default) | `source-checked` | `coding-checked` | `disputed`. **The load-bearing distinction is `source-checked` against `coding-checked`:** the latter records that a reviewer accepts the reasoning but did not or could not verify it against the source, which is the honest state wherever `source_lang` falls outside the reviewer's languages. Without it the twenty-three Turkish `avariz_vakfi_kirkcesme` rows could only be marked reviewed or unreviewed, and both would be false. **Why the defaults are substantive and not `.NR`.** The schema declares `missingValues: ['', '.NR', '.IL', '.NA']` globally, so a `.NR` default would be read as missing and no enum would bite. More importantly it would be a **category error**: `.NR` means *the source does not record this*, a claim about the world, whereas these three fields are claims about the project. The missingness discipline is the one convention this repository has held to without exception and it should not be overloaded with process metadata. `.NA` is the single exception, used only where the row genuinely has no source. **Constraints verified by injection**, not assumed: a bogus `review_status`, a bogus `source_read` and an empty `reviewed_by` each raise a `constraint-error` and turn the package `INVALID`. This check was prompted by noticing that `confidence` carries `.NR` in five hundred rows despite an enum of `high|medium|low` — which turns out to be correct behaviour, since `.NR` is a declared missing value, but was worth establishing rather than assuming.
- **`avariz_vakfi_kirkcesme` coded on 23 characteristics** (`LM-0489`–`LM-0511`), from Küçük 2025 (`10.21021/osmed.1472954`), read in full: the Kırkçeşme *mahalle* avarız akçesi endowment at Kastamonu, from its manuscript *vakıf defteri* in the Milli Kütüphane with the kaza's avarız records, population registers and *şer'iyye sicilleri*; disbursements dated 1841. `loss_mitigation_forms` **0.4.0 → 0.5.0** (511 rows, 33 forms). **The census's first Islamic form and its third civilisational bloc.** **Turkish-language source.** Coded `source_lang=tr`. **The blanket confidence cap agreed for this batch earlier the same day is withdrawn, and the reasoning behind it was wrong rather than merely superseded** — capping `confidence` because the reviewer could not read the language conflates evidential strength with reviewer availability, and the latter now has its own fields (`reviewed_by`, `review_status`). A figure quoted unambiguously from an account book is not weaker evidence for want of an auditor. The confidences stand as coded, having in practice been set on evidential grounds: `RB3=low` is a source-critical judgement about scribal formulae, `GV1=.NR` is Küçük's silence. MS reports the Synergy team will include a PI reading Turkish and Arabic, so these rows are reviewable in principle and are an early target for `review_status=source-checked`. **`CN1=.NA`, and it is the most informative cell in the batch.** Nothing is collected from those exposed. The fund is capitalised once — 6,280 *kuruş* of *anapara* endowed on the quarter — and lives on a 942 *kuruş* annual *nema*, a hair over fifteen per cent. Every other membership fund in this census assembles a stream; this one does not. `CN2`, `CN3` and `LS2` propagate `.NA`. Second form after `warichi_iwade` to take this signature, and the first to take it while still relieving a peril. **`MC1=allocation`, and this is the census's third contributor–beneficiary disjunction — the falsification condition recorded on 2026-08-12 is met.** The capital is donated by "those whose circumstances are comfortable"; the intended beneficiaries are those "without the ability to pay". Donor and beneficiary sets are close to **disjoint** here, where `guild_box_dutch` and `friendly_society_female_england` merely overlapped imperfectly. Three cases, so **`MC1` now needs the companion characteristic recording whether the contributor set and the beneficiary set coincide** — as its own commit, per the standing rule, not folded into this batch. **Split candidate recorded and not coded:** Küçük states these funds were capitalised *either* by wealthy donors *or* as an aid fund the inhabitants formed among themselves, and the second variant would take `pooling`. One row cannot hold both; Kırkçeşme evidences only the first. **`LS1=capped` on the hardest capital-lock-in rule in the census, stated as a principle and enforced by personal liability.** "Expenditure must absolutely be made out of the profit obtained; the principle that no expenditure be made from the principal was adopted" — and even the officials collecting the *avarız akçesi* were to indemnify the fund from their own property if principal were spent. So loss falls where the year's return runs out, with the corpus untouchable. Note the asymmetry `LS3` cannot express: liability beyond contribution attaches to the **administrator**, not to the beneficiary. **`HZ1=mixed`, the first form holding both hazard classes explicitly.** Covariate — earthquake, flood and fire, with victims relieved after the event — and idiosyncratic — paupers' funerals, marriage portions for orphaned and destitute children, and the tax share of the individual household that fails. The fiscal obligation is covariate to the point of perfect correlation, since the levy is assessed on the quarter as a body; **which** household fails is idiosyncratic. **`RB3=mixed`. NOTE, CORRECTED: this does *not* give `surety` its first occurrence — an earlier draft of this entry said so and was wrong.** `surety` and `personal` both still stand at **zero occurrences** as values, because the rule here admits either or both and is therefore coded `mixed`. The widening of 2026-08-12 has now been in place for two days and two of its five new values remain unoccupied. Borrowers furnished "one of, or both together" a *rehn-i kavî* (strong pledge) and a *kefil-i melî* (solvent guarantor), at the fifteen per cent *muamele-i şer'iyye* rate, under the *kadı*'s supervision, on terms not exceeding a year. The values are the tradition's own two categories, not analyst impositions.
- **Çizakça 1995 (`10.1163/1568520952600407`) read in full via OCR — image-only PDF, rendered at 300 dpi and passed through tesseract — and recorded on the `cash_waqf` type row rather than coded.** English, and therefore auditable by MS, which under the language rule makes it the source that should carry load-bearing cells here in future. **A survival statistic the census can use, and it is the first hard number on perpetuity in either dataset.** Of 761 individually identified Bursa cash waqfs traced across the registers, **148 survived more than a century — a "perpetuity percentage" of 19 per cent**, which Çizakça calls a probable minimum since waqfs whose *mahalle* the clerks omitted could not be traced. He is careful that this only half-answers Ebussuud's claim that cash waqfs had as good a chance of survival as real-estate ones, because the survival rate of real-estate waqfs "has never been attempted" — and notes that their staying power should not be exaggerated either, given fire, earthquake, declining fertility, inelastic rents and tenants who could not be evicted. **Relevant to `DR1=open-ended`, which the census records as a design intention and has never tested against realised duration.** **A historiographical dispute held open, not resolved.** Whether cash waqfs lent at interest is contested between Barkan–Ayverdi, Mandaville and Gerber on one side and jurists arguing the historians are confused by the deeds' terminology on the other. Çizakça tests it through *murabaha*/capital ratios. **Not coded either way**, on the `warichi_iwade` precedent. **Bursa's own *avarız-ı mahalle* funds appear in his dataset** — Abdal Mehmet, Ahmet Dai, Alaca Hırka, İncirlice and others, with capital figures across 1078, 1163, 1181, 1201 and 1239 AH. **A longitudinal series of neighbourhood-fund capital, which is the obvious test of whether these endowments eroded**, and a candidate second form.
- **`cash_waqf` added as a parked parent type row**, from Deniz 2026 (`10.33227/auifd.1740727`), read in full on 97 sixteenth-century *vakfiyes* from 35 registers. **Boundary decision recorded: the cash waqf is a funding vehicle whose purpose varies, not a hazard pool.** Of the 97, seventy-seven are fully charitable, three pure family endowments, the rest hybrid; named beneficiaries are imams, students and the poor. The same instrument endows a mosque, a lineage, or a quarter's collective obligations, and only the last is loss mitigation. Its place is `organizational_forms` beside `waqf_khayri` and `waqf_ahli`. Declared `same-institution` with `avariz_vakfi_kirkcesme`, which *is* one.

### Changed

- **`RB3`: the widening of 2026-08-12 is now half-validated and half-challenged, from the same source.** **Validated.** Deniz's 97 deeds distribute over the tradition's own two security categories with measurable frequencies: about half stipulate no security at all, roughly a quarter require pledge **and** guarantor, about a fifth guarantor only, four deeds pledge only. Özcan's transaction records give a different shape — 18 per cent none, 62 per cent guarantor only, 7 per cent both, 11 per cent pledge only — so **deeds and practice diverge**, which is a finding in its own right and a caution against coding a form's `RB3` from its constitution alone. **Challenged: `personal` and `none` may not be discriminable.** Roughly half of Deniz's deeds stipulate no security; lending on the borrower's personal credit and lending without security are one fact described twice. `personal` still has **zero** occurrences and it is no longer obvious what would produce one. **I argued for this widening; the first source able to test it suggests it over-split.** Falsification condition: a source distinguishing an advance made against the borrower's standing from one made against nothing at all. Until then `personal` should be treated as a value awaiting justification, not a gap awaiting a case. **CORRECTION, SAME DAY, AFTER ÇİZAKÇA 1995: the legal-suppression claim below is over-generalised and is retracted as stated.** Çizakça, working from Bursa court registers across 1555–1823 — **transactions, not deeds** — describes *istiğlal* as the ordinary cash-waqf lending device in seventeenth-century Bursa: "the borrower handed over to the lender a piece of real estate, supposedly as a sale, but actually in pawn. If the borrower redeemed his debt after one year, the asset reverted to him," the lender meanwhile leasing it back at a "rent" of often exactly ten per cent — "a simple interest bearing loan **with a piece of real estate as security**." Kaya & Akkaya report the same devices, *bayʿ bi'l-istiġlāl* and *farağ bi'l-istiġlāl*, running eighteenth-century Üsküdar. **So real estate was routinely taken as security by cash waqfs, under a name that does not contain the word *rahn*.** The prohibition Deniz reports was aimed at a specific abuse by *sipahis* and tax officials in Rumelian districts; it did not extinguish the practice, and I generalised one source's regional measure into a rule for the tradition. **The provinciality of `RB3=real-estate` in this census is therefore a fact about what has been read, not about Ottoman law.** Left as originally written below, marked, per the standing practice of keeping superseded reasoning auditable. ~~**`real-estate` is provincial for a reason, and the reason is legal suppression.**~~ Of Deniz's four pledge-only deeds, three restrict the pledge to gold and silver, excluding real estate — "normally the most common form of pledge". From the later sixteenth century creditors had been seizing peasants' land in pledge, forcing unpaid labour and distress sales, "often leading to bankruptcy. As peasants fled their lands, agricultural decline threatened state revenue," whereupon the state prohibited taking real estate, vineyards and gardens in pledge and ordered those already taken returned. **So the census's only `real-estate` being Japanese is not a sampling accident: in this tradition the value was legislated out of existence, and for a reason directly about the absorbing barrier.**

### Fixed

- **Audit of van Leeuwen 2012 against the 22 `guild_box_dutch` cells that cite it — the census's most-cited source, never previously opened. Five cells wrong or overstated, four of them coded `high`.** See logbook 4 for the full table.
  - **`VF2` `loss-occurrence`/high → `mixed`/high.** The certificate establishes incapacity but is not the only thing established. Van Leeuwen p. 68: assistance was denied if the applicant "had not been a member long enough, or if he had been sick too long or too often, was behind in paying his premiums, **had incidental earnings that rescued him from poverty**, or if he led an unseemly life — if, for example, he had an illness (such as venereal disease) or injury (perhaps incurred in a fight or through being drunk) **attributable to reprehensible conduct**." Fault, need and compliance alongside occurrence. **This is the fourth `mixed`, which meets the falsification condition set the same day for splitting `VF2` into a ternary per object — do it as its own commit.**
  - **`PY3` `1`/high → `P`/high, and the source says the opposite of what was coded.** Leeuwarden's bricklayers paid 100 guilders to one recipient, 80 each to two, 60 each to three, 40 apiece beyond; a Leiden widows' fund ruled in 1738 that past eleven or twelve widows "the widows will have to be content with receiving as much less as there shall be more widows"; and "if money was tight, guild administrators would not hesitate to cut allowances." Duration is equally soft — capped in three of seven schemes in table 9, uncapped in four, and at p. 68 extended past the rules or "restricted when the guild's resources were low". **Third `P` on `PY3`, and the three are different shapes: division by claimant count here, taper by claim duration in `friendly_society_female_england`.**
  - **`MB3` compulsory `high` → `medium`.** Table 9 makes compulsion a property of some schemes and not others — yes for the Amsterdam surgeons, shipwrights and peat-porters and the Utrecht master carpenters, **no** for the Utrecht journeymen carpenters association, the Leiden journeymen woolcombers and the Beurs der Leydsche Gerechtigheyd. The cut appears to fall between masters' and journeymen's bodies and this row smears it. **Split candidate, and it bears on the compulsion count directly.**
  - **`PR1` `0` `high` → `medium`.** Van Leeuwen introduces the Haarlem burial fund under "Differentiation in contribution level by risk category ... This happened on occasion": free entry between 12 and 40, and above 40 "three guilders for each year that aforesaid person is over the age of forty years". `0` is retained because that is a voluntary burial fund rather than a guild box, but the boundary is doing real work and the source treats them as one system.
  - **`MC1` note strengthened — it was *understated*.** Van Leeuwen's own verdict on the *noodhulpen* is that the guild "took full advantage of its position as official organizer of labour in a corporative political economy **to exploit non-members to the advantage of its own coffers**", with the Amsterdam peat-porters taking "no less than a quarter of all its income from substitutes" in 1750, plus civic levies on wharf owners, peat ships and incoming wood — so that at their zenith members "probably paid no more than half of their welfare costs from their members' contributions, and sometimes considerably less." **`LS1=pro-rata` and `MB2=1` were confirmed at `high`.** `LS1` is now the best-evidenced instance in the census. **Method finding.** Four `high` confidences survived from an unread source into the codebook, and **no check in this repository could have caught any of them**, because no check compares a cell to a page. Proposed standing rule: *no cell carries `high` on a source the coder has not read in full*, and `[verify]` is not a substitute for reading. `widows_fund_scotland` should be swept next on the same grounds.
- **`avariz_vakfi_kirkcesme GV1` corrected from `external-administered`/`high` to `.NR`, same day.** I coded it on the reasoning that "the beneficiaries do not choose him" — **an inference, not evidence.** Küçük names the *mütevelli* and gives the selection *criteria* but never says who selects. Kars 2020 on the Istanbul funds of 1618–1697 states that administrators "were chosen by the people of the quarter and registered by them at court", which is `collective` and the opposite of what I wrote. A different fund in a different century cannot fill the cell, but it destroys the inference that filled it. Held `.NR`. **The kadı's power to replace a *mütevelli* on proven betrayal is oversight, not appointment, and does not settle `GV1`.**
- **`avariz_vakfi_kirkcesme RB3` confidence cut from `high` to `low`, same day, on a source-critical point that bears on every `RB3` cell coded from a constitution.** Çınar 2017 shows that *"rehn-i kavî veya kefîl-i melî ya da ikisinden biri"* is **a drafting formula, not a description of practice** — present in almost every cash-waqf deed, "klişeleşmiş ve bir formüle dönmüş", stereotyped into a formula, which is why Çınar omits examples of it from his own study. **Küçük's wording for this fund is that formula verbatim.** So what I coded at `high` may be a clause copied by a scribe rather than security actually taken at Kastamonu. **This also explains the Deniz/Özcan divergence recorded above** — deeds and transaction records disagree because deeds are formulaic and transaction records are not. **General rule now established: `RB3` from a deed records drafting convention; `RB3` from an account of actual advances records practice; the two must not be pooled.** And the `personal`/`none` problem is now worse than stated this morning: if the security clause is boilerplate, its *absence* in roughly half of Deniz's deeds is evidence about scribes, not about lending. `surety` retains its first occurrence but at `low`, and both it and `personal` need a source describing advances actually made.
- **`LS1` note extended: the cap is a first-loss layer, not a limit on the members.** Kars supplies the shortfall rule — the fund's income is applied to the levy, and "when the income does not suffice the people of the quarter pay the remaining part by sharing it among themselves." The fund's liability is capped at its annual *nema*; the excess reverts pro-rata to the households. `LS1` holds one of those and not the layering, and `capped` is retained because the fund is the unit coded — **but the members are not capped, and reading the cell as though they were would invert the risk-bearing.** Kars independently confirms the untouchable corpus from a different century.
- **`MC1` note corrected: "close to disjoint" overstated the contributor–beneficiary gap.** Kars is explicit that founders relieve **their own** quarters, the donors being "those of means among the notables of the same settlement unit." So this is the York honorary-member structure of `friendly_society_female_england` — contributors inside the community, outside the beneficiary class — rather than the outsider funding of `guild_box_dutch`. Still the third case and still the reason `MC1` needs its companion, **but the three are not the same shape, and the companion characteristic must be designed to distinguish them.**
- **`HZ1` note extended.** Kars adds *salgın hastalık*, epidemic disease, to the covariate list beside fire, earthquake and flood — the census's first epidemiological peril — and the capital needs of those entering business to the idiosyncratic side.
- **`PY0=P` proposed and withdrawn before merge.** The Kırkçeşme fund outputs both a collective good (upkeep and staff of the *ulu cami*, *medrese* and *imarethane*, and the quarter's collective obligations) and individual indemnities (named disaster victims, paupers' funerals, orphans' marriage portions). `P` is the right shape and is **not available**: it has never been used on a nominal characteristic in this census and `PY0`'s `allowed_values` do not admit it. Resolved as `individual-indemnity` at `low`, with the tension carried in the row note — confidence taking the strain that the value set cannot. Caught by checking existing usage against the vocabulary, not by validation, which would have failed at merge.
- **`friendly_society_female_england` coded on 23 characteristics** (`LM-0466`–`LM-0488`), from Rusnock & Dietz 2012, *Journal of Women's History* 24:1, 60–85, working from thirty-nine sets of printed rules for English female friendly societies, 1780–1830. `loss_mitigation_forms` **0.3.0 → 0.4.0** (488 rows, 32 forms). **Coded as its own form rather than used to fill `friendly_society_england`'s thin cells, reversing an instruction this repository had written to itself.** The `friendly_society_england` type note said Rusnock & Dietz "would fill CN2 and PR1"; that was written before the source was read. Prom's lodges are male or mixed, 1870–1914; these are women's clubs a century earlier. Filling one form's `.NR` from another form's source would have manufactured agreement between populations never observed together. **The two forms now separate on `CN3` (`P` / `1`), `PY3` (`1` / `P`) and `PR1` (`0` / `P`)** — three degrees of freedom that filling would have destroyed. The superseded instruction is retained, marked as superseded, on the `friendly_society_england` row. **`PY3=P` is the census's first tapered benefit.** Five shillings a week for four weeks then two "until she recovered" (St Mary Redcliff); three shillings for thirteen weeks then *"1s. a week only, while it is thought necessary"* (Hampshire); St Augustine reduced after a month, capped the reduced relief at six months, and barred re-application for two months *"unless Seized with a Fever."* `PY3`'s ternary asks whether the payout is capped and cannot express a cap whose shape changes over the life of a claim. **The strain is recorded, not repaired** — one form. **`PR1=P` is the clearest ex-ante peril pricing in a membership fund so far, and it is priced on health as well as age.** Entry screening excluded anyone with "any distemper or sickly constitution" or "no bodily disorder or weakness"; "No lame, no infirm, would be admitted"; one set of rules required prior smallpox; concealment at admission was immediate grounds for exclusion. Age was capped at forty or thirty — **and where the bar was relaxed it was relaxed for a price**, older women taken "only in exchange for higher entrance fees." A charge graded against expected claim without any table, which is the reading given to `tontine_fr_royal`. Fourth `P` on `PR1`, first arising from a health screen. **`PY1=realised-loss` with the trigger defined functionally and articulated as such:** "to be sick meant to be unable to work: illness was defined in functional rather than medical terms," the Bristol formula being "sick or any way affected by the Hand of God, and unable to work." Note the exclusions this produces — Earl-Shilton confined full benefit to illness that "must speedily terminate in recovery or death," while "Blindness, habitual lameness, insanity" did not qualify because "such conditions did not interrupt a normal working life, they precluded one." **`VF1=communal-attestation` / `VF2=mixed` — the second use of the split, in a second tradition and a second period.** Stewardesses, in office "through either election or strict rotation" for terms of three months to a year, visited the sick "to verify her complaints and pay her benefits," daily in the largest Bristol society; and "If one of the daily visitors found a sick member working, her relief would be terminated immediately and an official hearing convened." One office, one visit, two objects — the exact pattern that forced the split on 2026-08-13, now independently attested. **`MC1=pooling` held at `medium`, on a new ground.** These societies had constitutional non-beneficiary contributors: York divided the membership into "Two Classes, viz. Honorary Members" and "General Members," the honorary paying six shillings annually for a committee seat while "exempt from fines and received no benefits"; Stafford's 1797 roster shows 65 honorary against 111 subscribing. Second mechanism by which contributors fall outside Harris's pooling as written, after `guild_box_dutch`. **Falsification condition recorded: a third such form puts the contributor-set/beneficiary-set question to `MC1`.**
- **Two sources read in full and deliberately not coded**, with reasons in logbook 2. **Lavallée, Bénézet & Julien 1993 — excluded, and the catalogue description of it was mine and was wrong.** Briefed into this project as "the Rouen pharmacy guild"; *la maîtrise Notre-Dame de Rouen* is the cathedral **choir school**, and the article is about the chapter reimbursing the medical costs of its *enfants de chœur* — bills to parents, to the *maître de musique*, to the widow Vauquelin for nursing, to a butcher for meat. **No membership, no contribution stream, no fund assembled from those exposed**: `MC1` fails at the first question and `CN1` would be `.NA` for the same reason `warichi_iwade`'s is. I repeated a search engine's one-line summary into repository prose without opening the source, the second time in this sweep (after Goddard & Smalley). Recorded as a methodological rule in logbook 2. **Klieber 2001 — excluded on the source's own finding, which is worth more than the coding.** Acquired to extend `confraternity_fund` past Gazzini's Italian evidence; it shows the opposite. Post-Tridentine Salzburg fraternities are death-service bodies whose material relief is "ein eher bescheidenes und eindeutig nachrangiges Engagement" — one *Liebesbund* reserved up to ten per cent of expenditure for alms, and only from the 1770s — and Klieber states that the charitable dimension "hat sich in romanischen Ländern offensichtlich stärker erhalten." **Consequence recorded on the `confraternity_fund` type row: it cannot be coded as one row for Latin Christendom and must be split regionally, on the `tontine_en`/`tontine_fr` precedent.** Also not coded because the PDF's text layer is column-interleaved and the contribution-rate passages are not reliably legible; it needs the image route.

### Fixed

- **Batch merged with scratch record IDs (`LM-W-01`…) that violate the schema pattern `^LM-[0-9]{4}$`.** Caught by `frictionless validate` — 23 constraint errors — before any downstream artefact was regenerated. Previous batches were renumbered into the sequential `LM-####` range on merge and this step was skipped. Renumbered to `LM-0466`–`LM-0488` and the logbook reference updated. **The scratch-ID convention is useful for drafting and is a merge hazard; the schema is what catches it, which is an argument for running `frictionless` before, not after, anything else.**
- **`VF1` split into mode and object; new characteristic `VF2 verification object`.** `loss_mitigation_forms` 0.2.6 → **0.3.0** — a characteristic addition, hence the minor. Fifteen `VF2` rows (`LM-0451`–`LM-0465`), one for every form carrying a `VF1`. **This is a repair of a conflation, not added resolution, and it was declined twice before it was made.** `CHARACTER-CODING.md` says a characteristic earns its place only by repairing a conflation in Sereno's sense — one characteristic silently asking two questions — and that discriminant validity cannot be settled analytically. On 2026-08-12 the split was proposed and **discarded** because mode and object came apart in only one form; the reasoning is in `logbook/4`. Prom 2010 supplied the missing evidence the following day. **The evidence, stated as the pairing it is.** One mode takes three objects; one object takes three modes; and in `friendly_society_england` a communal mode carries a dual object *within a single form* — the sick visitor goes "to offer moral support for the ill member, **or** to investigate suspected fraud", while lodges banned drinking, being outdoors or working while on the box.

  | form | `VF1` mode | `VF2` object |
  |---|---|---|
  | `guild_box_brabant` | official-adjudication | loss-occurrence |
  | `guild_box_dutch` | documentary | loss-occurrence |
  | `guild_relief_england` | official-adjudication | mixed (need **and** fault) |
  | `craft_pension_edinburgh` | official-adjudication | claimant-need |
  | `friendly_society_england` | communal-attestation | mixed (condition **and** conduct) |

`check_dependence.py` reports the pair as **separated on substantive values**: "VF1=official-adjudication occurs with VF2 in ['claimant-need', 'loss-occurrence', 'mixed'] — VF1 does not determine VF2".

**`VF2` values**: `loss-occurrence` (the defined misfortune happened or persists) · `claimant-need` (reduced to want, irrespective of the event) · `claimant-fault` (the misfortune did not arise from the claimant's own misconduct) · `conduct-compliance` (the claimant observes the conditions of relief while drawing it) · `mixed`.

**Ontological dependence declared and enforced.** `VF2 applicability_on VF1` — an object of verification presupposes that verification occurs — with `BLOCKING[("VF2","VF1")] = {"none"}` registered in `check_dependence.py`, since that table is where applicability gates actually live. **Verified by injection**: setting `guild_box_brabant VF2=.NA` against a substantive `VF1` raises `SUSPECT … does not block, yet VF2=.NA` and a non-zero violation count; reverted. Both are also declared `dependence_group=verification, scope=conditional`, so the separation is reported rather than assumed on every future run.

**`VF1`'s definition rewritten** to say it names the mode only, with the split and its warrant recorded in the definition itself. **Seven `VF1` row notes updated** to point at `VF2` and to record that the repair was a split and **not** the fourth state those notes had proposed — a fourth state would have entrenched the conflation by adding a mode to describe an object.

**Two cells deliberately left `.NR` and flagged.** `averia_pool` and `general_average` both carry `VF1=official-adjudication` coded before the split, and their objects were never asked about. `general_average`'s is very likely `loss-occurrence` — a jettison either happened or did not — but that must be read from source rather than assumed, and both are named in `logbook/4` as owing a return to the sources.

- **`friendly_society_england` coded on 22 rows** (`LM-0429`–`LM-0450`), from Prom 2010, *Friendly Society Discipline and Charity in Late-Victorian and Edwardian England* (*The Historian* 72:4, 888–908, `10.1111/J.1540-6563.2010.00280.X`), read in full from Oddfellows and Free Gardeners lodge minute and fines books in Durham, Northumberland and Yorkshire. `loss_mitigation_forms` 0.2.5 → **0.2.6**. 450 rows, 31 forms. **`VF1=communal-attestation` — the value's first use anywhere in the census.** The verifier is neither an officer nor a document: the sick visitor is "the fellow member sent round to offer moral support for the ill member, or to investigate suspected fraud". **And this form settles the `VF1` question that was left open on 2026-08-12.** The split was declined then for want of discriminant-validity evidence: mode and object came apart in only one form. They now come apart across five and, decisively, **within this one**. The sick visitor's mode is communal and his object is *dual* — the condition, and the claimant's conduct, since lodges banned "drinking, being outdoors, or working while receiving sick pay". Set against `guild_box_brabant` (official mode, condition object), `guild_relief_england` (official mode, fault object) and `craft_pension_edinburgh` (official mode, need object), the two vary independently. That is what Campbell & Fiske require and what was missing before. **`logbook/4` updated; the falsification condition set there is met and the split should now proceed as its own commit.** **Enforcement as documented practice rather than rule, which is a finding in itself.** `CN3=P` because the rule was enforceable and was not enforced: "Although national leaders tended to discourage the practice, members were usually granted sick pay and medical attendance even if they had fallen weeks behind in their payments." Visitors "turned a blind eye"; Benjamin Goodwin warned members when he would be coming round; the Durham Loyal Star of the North let a member keep a month's sick pay after he quietly went back to work part-time. But breach of trust was punished savagely — George Wardle fined £6, a de facto expulsion, "after he falsified his eligibility for sick pay and medical attendance", and expulsions are recorded for gambling, for resuming work in the pit, and in one case for helping to feed his child while on sick pay. First form in the census where a contribution rule is documented as systematically unenforced. **`MB2=1` with the bite measured.** Nearly every society "banned new members after that age [forty]", so expulsion past forty "essentially left members … unable to secure any sickness insurance or medical care for the rest of their lives". An adverse-selection control whose severity is visible in the impossibility of re-entry. Coded from a study of discipline, so contribution rates and actuarial structure are absent — `CN2` and `PR1` are thin, and Rusnock & Dietz 2012 and Broten 2012 are in the collection to fill them. `HZ2=P` repeats the conflation recorded for `guild_box_dutch`: the illness is indifferent, the claim is not, and the characteristic still cannot tell endogenous claiming from an adversarial hazard.
- **`craft_pension_edinburgh` and `craft_widows_fund_edinburgh` coded on 42 rows** (`LM-0387`–`LM-0428`), from Allen 2022, *Corporate Charity for 'the House'* (*Scottish Historical Review* 101:2, `10.3366/shr.2022.0561`), read in full from the incorporation of Mary's Chapel minutes and discharged accounts, ECA SL34. `loss_mitigation_forms` 0.2.4 → **0.2.5**. 428 rows, 30 forms. **One institution, two regimes, and the pair is the point.** Compared on twelve cells they differ on **five** — `MB3` compulsory against voluntary, `PY1` need-assessed against life-event, `PY3` capped against uncapped, `LS2` **0 against 1**, and `PR1` 0 against P. Allen's argument is that the 1768 scheme existed precisely to convert discretionary charity into entitlement, and the coding registers that conversion as a reversal on `LS2`. **`LS2=0` is the hinge and it is articulated.** A pension under the old regime ran "during their pleasure" — Margaret Smibert was enrolled in 1728 at £4 Scots quarterly on a petition representing that "by reasen of old age and infirmitie of bodie she was altogether disabled to subsist her selfe and there by reduced to extream want and poverty", the incorporation "takeing her circumstances to their seriouse Consideration". No claim on the fund, revocable at will. Under the 1768 scheme subscribers' widows hold a claim on the fund's annual produce as of right. **`PY1=need-assessed` for the second time**, after `guild_relief_england`, and from a completely different archive — which strengthens the finding that need-assessment is a real position in this family rather than an artefact of McRee's normative sources. **`PY3=0` and `LS1=residual-on-fund` because the annuity floats.** Once the £5,500 capital was raised "the widows were to receive an equally divided portion of its annual produce", so the payment tracks the fund's income and the number of claimants rather than being fixed. Shortfall is absorbed by the size of each portion and there is no unmet loss to distribute. `PY2` is `.NR` with the reason stated: equal division of a fund's produce among all eligible claimants is not rotation, auction, indemnity or need-assessment, and `PY2` has no state for it. **`PR1=P`, and the distinction is worth having.** Actuarial expertise is directly present — the scheme was built with Dr Alexander Webster, an architect of the 1744 Scottish ministers' fund that Hare and Scott call the first actuarially-based fund in the world, and Allen calls the response "decidedly actuarial". But the mechanism described is capital accumulation against a target with the produce divided equally, a savings pool rather than a premium priced against a survival function, and Allen nowhere says contributions were age-graded. **Expertise attaches to the design process, not demonstrably to the instrument.** **A correction to an earlier expectation, recorded because it was published in this file.** Allen was scheduled as the source that would verify `widows_fund_scotland`, the census's only `PR1=1`. **It does not.** His subject is a craft incorporation; he cites the Ministers' Widows' Fund of 1744 but does not study it. That cell still rests on a `[verify]` citation and the fix is Hare & Scott 1992, which is not in the collection. **`VF1` gains a third object of verification and the logbook condition is partly met.** `craft_pension_edinburgh` verifies **need** — the petitioner's circumstances — where `guild_box_brabant` verifies the fact of incapacity, `guild_box_dutch` documents it, and `guild_relief_england` adjudicates fault. That is a second form verifying something other than the fact of the loss, which meets the general form of the condition set in `logbook/4`, though not its specific terms, which named a second **fault**-verifying case. Recorded in the row; not acted on. `friendly_society_england` remains the test.
- **`guild_relief_england` coded on 21 rows** (`LM-0366`–`LM-0386`), from McRee 1993, *Charity and Gild Solidarity in Late Medieval England* (*Journal of British Studies* 32:3, 195–225, `10.1086/386030`), read in full from the returns to the royal inquiry of 1388–89. `loss_mitigation_forms` 0.2.3 → **0.2.4**. 386 rows, 28 forms. **This form was acquired as a falsification test and it falsified.** The previous entry recorded that `guild_box_brabant` and `guild_box_dutch` agree on fifteen of fifteen comparable cells and flagged the possibility that the instrument, rather than the institutions, was doing that. English religious gild relief separates from both on **five and six cells respectively** — `MB1`, `MB3`, `PY1`, `PY2`, `PY3`, and `VF1` against the Dutch case. The near-identity of the two boxes is therefore a fact about those two institutions, not blindness in the characteristic set. **`PY1=need-assessed`, and it is the discriminating cell.** The occasion is a realised misfortune but the entitlement does not follow from it: "Gilds did not offer to replace valuables that had been lost or destroyed, but only to help members with living expenses when such losses had reduced them to **penury**." The Ludlow Palmers' Gild return makes the assessment two-sided and caps its repetition — relief "once, twice, and thrice, but not a fourth time, as much help shall be given to them, out of the goods of the gild, as the rector and stewards, **having regard to the deserts of each, and to the means of the gild**, shall order". Wallis's reported claim that English guild provision was discretionary rather than organised insurance is confirmed here from the returns themselves. **`MB1=confession`**, against `occupation` for both boxes: these are religious fraternities that "drew their members from a variety of professions and made no attempts at industrial regulation". `MB3=voluntary`, against compulsory in both boxes — and in the Dutch case compulsion was shown to be what made the fund viable. **`LS1=pro-rata` independently from a third tradition**, which makes it the census's best-attested loss-sharing value at four forms. The Wisbech Gild of St John the Baptist paid "only as long as gild funds lasted"; the Roughton gild ruled that if more than one member needed help the weekly 1d. "would have to be divided among all those in need". **`VF1` now has a well-formedness problem rather than a missing state, and three forms make it visible.** `guild_box_brabant` establishes *the fact of the loss* by a visiting officer; `guild_box_dutch` documents it by doctor's certificate; this form adjudicates **fault** — Stratford-on-Avon relieved poverty that was "undeserved", Cambridge St Mary denied aid to anyone impoverished *si non per latrocinium meretricium seu aliam viam malignam*, a London gild cut off any member caught begging. Those are three different **objects** of verification under a characteristic whose values name only **modes**. In Sereno's terms `VF1` is asking two questions at once and should be **split**, not extended with a fourth state as earlier rows proposed. No further form should be coded on it until that is settled. **Two further cells could not be set and both are value-set failures rather than silences.** `PY3=P` because most gilds fixed the allowance while Beverley's alderman and seneschals varied it case by case "anywhere from 4d. to 8d. per week" — and because Ludlow's cap is on **frequency**, which `PY3` cannot express at all. `GV1=.NR` for the third time: decisions rest with named officers, rector and stewards or alderman and seneschals, which is neither collective assembly, nor founder-fixed, nor external administration. **Source-type caution recorded in the type row.** The 1388–89 returns record what gilds *promised*. McRee warns that gild leaders had reasons to shade their answers, so these codings describe normative provision and not necessarily practice. **Correction, 2026-08-13:** Goddard & Smalley's Nottingham accounts were named here as the corrective. Having read them, they are not — the article treats the guild's finances and the cult of death and contains no discussion of relief to members at all. The corrective for the promise/practice gap in the 1388-89 returns has still to be found.
- **`guild_box_dutch` coded on 22 rows** (`LM-0344`–`LM-0365`), from van Leeuwen 2012, *Guilds and middle-class welfare, 1550–1800* (*Economic History Review* 65:1, 61–90, `10.1111/j.1468-0289.2011.00602.x`), read in full. `loss_mitigation_forms` 0.2.2 → **0.2.3**. **A second form rather than a completion of the first.** Van Leeuwen's material is the Dutch Republic 1550–1800; Masure's is Brabant 1250–1600 — different polity, different period, different archive. Filling `guild_box_brabant HZ1` from it would import evidence across forms, which this repository has refused three times this week. **The two are declared `cooccurs_with … uncertain`, not independent**: Masure records that such funds appeared centuries earlier in the Southern Netherlands and flourished in the North only from the late sixteenth century, so descent is plausible and they must never be counted as two cases in a comparative claim. **`HZ1` gets its first substantive value outside the maritime forms**, and it is `mixed`. Ordinary sickness is idiosyncratic; epidemic disease is not, and the funds legislated for it — the Amsterdam carpenters ruled that a brother taken ill "from the rapid sickness of pestilence, red measles, and suchlike" be confined to his house for six weeks, drawing benefit throughout. **`LS1` gets the census's clearest value.** Shortfall is shared pro rata by scaling every allowance down as claimants multiply. The Leeuwarden bricklayers' schedule of *c.*1750 tabulates it — one master alone draws 100 guilders, three masters with five widows draw 45 and 40 respectively, beyond that 40 per person — and a Leiden widows' fund of 1738 states the principle: "the widows will have to be content with receiving as much less as there shall be more widows." **`VF1` acquires a third value and the facet now discriminates.** `documentary` here — "Guilds almost invariably demanded a doctor's certificate, and often sent out someone to check on claimants at home" — against `official-adjudication` for the Brabantine *busmeester*'s home visit. Recorded caution: both practices run together in the Dutch case and `VF1` permits one value, so this coding suppresses the visitation. If a combined state is ever added, this row is the first to revisit. **`MC1=pooling` is coded at `medium`, and the hedge is substantive: the pool is partly funded by outsiders.** Harris's pooling requires mutualisation among stakeholders with no outsider. These funds fail that twice over. A sick member could have a stand-in (*noodhulp*) appointed who did his work for half the pay, the other half going to the member — the Amsterdam peat-porters drew a quarter of all fund income from substitutes in 1750, men who might remain substitutes for life. And city authorities granted guilds levies on third parties: the shipwrights taxed wharf owners, the peat-porters every incoming peat ship, the Utrecht carpenters incoming wood. At their zenith the rich Amsterdam guilds "probably paid no more than half of their welfare costs from their members' contributions, and sometimes considerably less." **A pool that transfers from non-members to members is not what `MC1=pooling` was written to describe** — a further strain on that characteristic, and the first from the funding side rather than the peril side. **`HZ2` exposes a conflation, coded `P` to record it.** The characteristic asks whether the hazard responds to the defence mounted against it, glossing `1` as adversarial (an enemy fleet) and `0` as indifferent (a storm). Illness is indifferent; its *claimed* incidence is not. The Dordrecht shipwrights complained in 1705 of brethren who collected sickness money and "immediately or not later than the following day … repaired once again to the street, without being inconvenienced by any sickness", and the funds answered with waiting periods — about a week for sickness, six months to a year for burial — certificates and home checks. **That is endogenous claiming, not an adversarial hazard, and `HZ2` cannot currently tell them apart.** **`MB3=compulsory` with the mechanism shown to be load-bearing**, which no other compulsory form in the census demonstrates. Compulsory guild membership "allowed the guilds to circumvent problems of risk selection"; the three non-guild associations van Leeuwen tabulates are all voluntary; and the Amsterdam carpenters made membership compulsory in 1708 because the fund would otherwise have been non-viable, relaxed it at abolition, reinstated it in 1806 when "financial necessity required", and went bankrupt in 1845 once compulsion became illegal. **`PR1=0` on the author's own testimony**, which is rare — premiums "were generally not calculated in an actuarially sound way". The contrast is in the same article: the Monnickendam widows' fund of 1754 charged age-related contributions when it "was actuarially possible at the time to calculate sound premiums", set them too low anyway, and failed. The technique existed and the guilds did not use it. Relatedly, `CN2=equal` is coded `medium` because a voluntary Haarlem burial fund demanded "three guilders for each year that aforesaid person is over the age of forty" — **the first risk-graded contribution anywhere in this census**, which `CN2`'s three values cannot express.
- **`guild_box_brabant` coded on 21 rows** (`LM-0323`–`LM-0343`), from Masure 2018, *Ambachtssolidariteit in Brabantse steden (1250-1600)* (`10.71265/q9temk46`), read in full. `loss_mitigation_forms` 0.2.1 → **0.2.2**. The craft guild sickness box of Mechelen, Brussels and Antwerp. **`VF1 loss verification mode` takes a substantive value in a membership fund for the first time.** It stood `.NA` on nine consecutive East Asian forms, every one of which pays by rotation, lot or ritual so that nothing is ever claimed. Here a sick member notified the *busmeester*, who made a *huisbezoek* — a home visit — to establish the claim and paid the weekly sum on the spot. Guild tokens (*gildepenningen*) evidenced membership, not the loss. Coded `official-adjudication` at `medium` **with the fit contested in the row**: the *busmeester* is an officer of the fund, elected from among the masters and accountable to the *ambachtsdekens*, not a public authority, and the value may have been written for the latter. It is not `communal-attestation` — no fellow member swears — and not `documentary`. If the vocabulary needs a fourth state for verification by the association's own visiting officer, this row is the evidence, and the question should be settled before the Italian and English cases are coded. **`PY1=realised-loss` for only the second time**, after general average, and the first in a membership fund. The Brussels smiths' ordinance of 1473 makes it an entitlement rather than a discretionary grant, in terms: *"Datter yemant inde voirs. bruederscap wesende zieck is, hij sij arm oft rijke, alle weke huer ziecte broot dragen oft sinden zelen moeten"*. Exclusions are equally explicit in the same year — nothing to anyone *"in openbaeren ouerspele sittende"* or going door to door for alms. **The cleanest `MC1=pooling` in the census.** After five forms whose `MC1` could not be set because they pool assets while moving no peril, this one mutualises a genuine hazard — incapacity to work through illness — across a membership, with no outsider. Note also that the box is a **segregated fund**: Masure describes these as institutions under the guild but with *"aparte boekhouding en bestuur"*, and says founding one was a way to *"de ambachtskas verlichten"*, to relieve the guild chest. Asset partition inside a corporate body, structurally the same move as the no-set-off clause in the 御手洗 銀頼母子. **Three cells record vocabulary strain rather than resolving it.** `MB2=1` is *closed but not capped* — Masure states there was no numerical limit and contrasts the almshouses, which capped at twelve to sixteen widows for the Mechelen skinners and twenty smiths in Antwerp — whereas every kō takes `MB2=1` for numerical restriction, so the cell now conflates two things. `MB3=compulsory` records a value that **changes within the form's period**, boxes moving from voluntary to *verplicht* for masters across the fifteenth and sixteenth centuries; coded for the mature form at `medium` and flagged for splitting on the tontine / ortoq / *kabu* precedent rather than hedged with `P`. `CN2=equal` is equal *within a benefit class*, since a Brussels master's wife could pay a higher fee to take the full master rate instead of three-quarters — third form after `ko_daikokuya_1848` and the Mitarai kō where `CN2` cannot express a tiered tariff. `PY3=1`, and the grading is the evidence for `PR1=0`: benefits differ by rank in the trade — a master drew one to two masons' daily wages a week, his wife about three-quarters "because her labour was valued lower", journeymen where admitted three-quarters — and not by any assessment of who is likelier to fall sick. An institution can grade, tier and calculate without pricing a hazard. **`HZ1` left `.NR` and named to a source.** Sickness is idiosyncratic on any reading, but Masure never asks about correlation across members. Van Leeuwen 2012 (`10.1111/J.1468-0289.2011.00602.X`) is reported to treat correlated risk and moral hazard directly and is the study that should fill it — the first substantive `HZ1` outside the maritime forms. **Metadata caution.** **Resolved.** The DOI resolves to *Noordbrabants Historisch Jaarboek* on platform.openjournals.nl, which is open access; the *Tijdschrift voor Sociale en Economische Geschiedenis* attribution reported by a full-text read is a misread of an internal reference.

### Changed

- **`guild_mutual_aid` split; "Japanese" removed from its tradition field.** The row read "Guild mutual-aid chest / European / Japanese", which stacked two conflations. It is now an annotated umbrella over four forms that take different values on trigger, entitlement and verification: `guild_box_brabant` (coded above), `confraternity_fund` (Italian, parked), `guild_relief_england` (parked) and `friendly_society_england` (parked), all declared `same-institution` against the umbrella. The Japanese side is dropped because Nagata 2008's material is already coded as `kabu_local` / `kabu_edo_export` in `organizational_forms`, and the mutual-aid function of Japanese guilds is a separate question not yet sourced.
- **`confraternity_fund` re-scoped and documented from Gazzini 2020** (`10.17464/9788867742967_04`), read in full but not coded. Aid is usually a *ragionevole aspettativa* rather than an enforceable right — the opposite value from `guild_box_brabant` on the entitlement question — and verification, where it exists, runs **ex ante**: the Milanese *Camera dei mercanti* checked which road a robbed merchant had travelled and excluded him if it was not an approved one. That is a moral-hazard control operating through a prior conduct rule rather than through *ex post* inspection, and the census has no other instance. Perils covered run wider than any coded form: illness, infirmity, old age, burial, unemployment, debt, work damage, theft of livestock, robbery on trade routes, ransom from pirates and bandits, legal costs, imprisonment. Code once the entitlement gradient can be set against van Leeuwen.
- **Chinese shareholding and Japanese *kabu* coded on 80 rows**, from Zelin 2019 (*Law and History Review* 37:2, `10.1017/S073824801800038X`) and Nagata 2008 (*IRSH* 53:S16, `10.1017/S0020859008003635`). `organizational_forms` 0.7.0 → **0.7.1** (+60 rows, `OF-0223`–`OF-0282`); `loss_mitigation_forms` 0.2.0 → **0.2.1** (+20 rows, `LM-0303`–`LM-0322`). Rows and census codes are additive, hence the patch. **`CI4 depersonalised interest` now takes the value `1` somewhere.** It previously did not, anywhere in the census — five forms, three `0` and two `P`. Objective 2 treats transferable claims as one of the corporation's five separable components and the dataset could not show that component reaching its endpoint on earth. `hegu_yingu CI4=1` is the first, on the Mutaishangtang dispute of 1911: the shop kept a share ledger, "each shareholder was issued a share certificate on which the number of shares held was clearly written," and "it was on the basis of these certificates that profits were distributed" — the Ying heirs lost because they had no certificate. `CI3` goes from six forms to eleven, `CI4` from five to ten. **Share classes, not firms, are the coding unit for the Chinese material.** A single Qing partnership issued 銀股 *yingu* silver shares (transferable, inheritable), 身股 *shengu* body shares (dividends, but **no equity claim and not inheritable**), 乾股 *gangu* dry shares (contributed land, **not liable for losses**) and 孤股 *gugu* death shares simultaneously — so one firm takes different values of `CI3`, `AP3` and `LR3` depending on which share is examined. Worse than the ortoq case, where the two contract types were alternatives; here the classes are simultaneous and internal to one entity. Coding at firm level would have erased the decomposability the whole of Objective 2 asserts. `hegu_yingu CI3=1` and `hegu_shengu CI3=0` now sit inside the same partnership. **A convention follows and is stated here so it is not rediscovered.** Where a row codes a share *class*, entity-level characteristics (`TS1`–`TS4`, `CI1`, `CI2`, `LP1`–`LP3`, `AP1`, `AP2`, `MG4`) are coded on the class that *is* the firm — `hegu_yingu` — and left `.NR` on the others with the mismatch named in the note, rather than answered about the wrong object. See `hegu_shengu TS1`. **The *kabu* split runs on territory and gives `FP2` its first unambiguous `1`.** A *kabu* is a licence to operate one shop, mill or scale, evidenced for brewing stock by a wooden tag; "the movement of shares outside the geographical region defined by the society was prohibited". `kabu_local CI3=P` — alienable, but only within a bounded class of licensed operators in one market. `kabu_edo_export CI3=1`: sake shipped to Edo carried a separately priced class that, "unlike the local shares, could be sold or traded outside the region or market of the original license." **The `P`/`1` distinction is principled, not a matter of degree**: a right of first refusal restricts *who* may buy within an open class; the territorial rule restricts *the class itself*. And the *kabu* **is** an exclusive right held as property, which is exactly what `ortoq_loan FP2=.NR` recorded that the ortoq's state-run maritime monopoly was not. **`CI3=1` survives restraints on alienation, per the maintainer's ruling and the `bazacle_mill` precedent.** Zigong well-drilling contracts commonly stipulate that "a partner may not transfer his shares to someone else on his own" and the Shenxin mill required unanimous consent, but Zelin reads these as rights of first refusal and records that Zigong shares changed hands frequently regardless. **The finding the pairing exists for.** Chinese *gufen* and Japanese *kabu* reach the same functional endpoint — a transferable, inheritable, certificated income claim — by opposite institutional routes. `hegu_yingu`: `MG4=private-contract`, `FP2=0`, `FP4=0`, in a legal order that "left the regulation of most economic transactions to the normative realm and private contractual ordering". `kabu_local`: `MG4=state-charter`, `FP2=1`, `FP4=1`, with unregulated societies banned outright and merchants *required* to join in 1721. Same function, divergent arbitrary particulars — the pattern the project's own diagnostic names as what carries independence, and the first place the census can set two East Asian traditions against each other on a WP2 component without Japan standing alone. **`shenhui_gu` is coded in both censuses**, the first cross-dataset pair populated through the `cooccurs_with` mechanism ported this week, following the repository's existing `*_alloc` convention (`shenhui_gu_alloc` in `loss_mitigation_type.csv`). It is **the census's first form combining a collective-good output with a tradable claim**: rents from a corporate deity estate fund a ritual banquet for all members — `PY0=collective-good` with `PY1`–`PY3` propagated `.NA`, the `averia_pool` configuration — while the share itself is bought, inherited and sold like land, and any surplus after the ritual is distributed to shareholders (`LS2=1`, articulated). **`MC1` cannot be set, and the reason is specific.** `shenhui_gu_alloc MC1=.NR` because no peril is at issue — the second such form after `ko_daikokuya_1848`. (An earlier draft of this entry called it the fifth, summing it with `particular_average`, which is `.NR` as the declared null case, and `warichi_iwade`, which is `.NR` because a historiographical dispute is held open. Three different reasons; corrected 2026-08-12.) Assets are pooled; no hazard is. `MC1`'s three values all presuppose a peril being moved among parties with a stake, and the census now holds a substantial class of cooperative arrangements that pool without one. Recorded for the logbook, not acted on. **`VF1` is now `.NA` on nine consecutive East Asian forms.** Every one has a payout trigger that is not a realised loss, so nothing is ever verified. The characteristic carries no information about this material at all and needs a form with `PY1=realised-loss` before it means anything. Two parked type rows: `hegu_gangu` (one Sichuan ironworks only) and `gugu_death_share` (Morck & Yang's sole example is from the 1940s and Zelin flags possible foreign influence). Reserved terminology observed throughout: both authors write "risk" for what these codings treat as hazard.

### Fixed

- **`check_vocabularies.py` rejects a legitimate cross-dataset reference when the code name is identical on both sides.** `check_cooccurrence` tests `target == code` *after* stripping the `@dataset` qualifier and without checking whether one was present, so `shenhui_gu` in `loss_mitigation_type.csv` referencing `shenhui_gu@organizational_forms` is reported as a self-reference. The documented `code@dataset` form is exactly what that reference should use. Not patched here — the repository's own `*_alloc` naming convention (`commenda_alloc`, `qirad_alloc`, `isqa_alloc`, `ortoq_alloc`) avoids the collision and was followed, so the bug is latent rather than blocking. The one-line fix is to guard the self-reference test with `not ds`.
- **`cooccurs_with` and `cooccurrence_basis` ported to `vocabularies/organizational_form_type.csv`**, with **`same-institution`** added to the basis scale above `same-instrument` as its tightest level. `organizational_forms` 0.6.1 → **0.7.0**: vocabulary structure moved, and this repository reserves the minor for exactly that. The asymmetry this closes was real and it sat in the worst possible place. `loss_mitigation_type.csv` gained these columns so that forms drawn from one community could not be silently counted as independent cases; `organizational_forms` had no such facility, and it is where the `qirāḍ` / `commenda` / `ʿisqa` / `ortoq` cluster lives — the most tempting place in the repository to run a similarity claim and the worst place for undeclared non-independence. `ortoq_equity` and `ortoq_loan` are now declared `same-institution` in both directions. **No change to `check_vocabularies.py` was needed**, and that is worth recording: `check_cooccurrence` iterates every vocabulary generically rather than naming files, so the port inherited enforcement automatically. Verified by injecting all three defect classes into the new columns — a dangling code, a `cooccurs_with` with `.NR` basis, and a self-reference — and confirming each was caught, then reverting. `ortoq_alloc` in `loss_mitigation_type.csv` now carries `ortoq_equity@organizational_forms;ortoq_loan@organizational_forms` at `same-institution`, which converts a prose cross-reference into a checked one through the documented `code@dataset` form. **Not populated beyond the ortoq pair.** The obvious next candidate is `waqf_khayri` / `waqf_ahli`, two branches of one institution, and it should be declared when `waqf_ahli` is assessed rather than asserted now.

### Changed

- **Six `ortoq_loan` cells revised and one added, from Endicott-West 1989 (*Asia Major* 3rd ser. 2:2, 127–154), now read in full.** She fills the two cells that were parked as hers, and constrains two more. **`FP4` `.NR` → `1`, high, `articulated`.** 斡脱戶 *wo-t'o hu* is a named household-registration category in Yuan law, and the tradition names it in its own decrees — 通制條格 2 quotes imperial 聖旨 and princely 令旨 "concerning the sundry ortoy households". Registers stand behind it: a 1252 original and an 1263 supplementary listing 2,953 Muslim civilian households at Chung-tu. So does a bureaucracy with a datable life — Bōral Qarasun placed in charge 1253-01-09; 斡脱總管府 established 1268-01-13; 斡脱所 regional offices 1272-09-05; renamed 泉府司 1280-11-30; abolished 1284, reinstated 1285 under Dašman; given the Maritime Trade Bureaus 1286; regional 行泉府司 from 1288; **abolished 1311 "apparently forever"**, after which the ortoy were "comparatively free of restrictions and controls". The cell therefore describes c.1253–1311 and the row says so. **`MG4` `.NR` → `state-charter`, medium**, on 吏學指南 (preface 1301) glossing *wo-t'o* as the name whereby government funds for trade were disbursed to earn interest, and on the 泉府司's remit over "the expenditure and income of government funds for the emperor as well as the heir apparent, the empress, and the princes". Medium because of the source's own limit, below. **Both left `.NR` on `ortoq_equity`.** Endicott-West's apparatus is Yuan-jurisdictional and Enkhbold's equity evidence is Persian and largely pre-1260. Since the periodisation was removed from these rows in this same cycle, mapping the Yuan apparatus onto the equity form would re-import the sequence reading through the back door. **The limit that matters more than the cells: "no commercial contract as such has come down to us in the Chinese sources of the Yüan period."** The liability coding therefore rests entirely on Persian narrative — Juvaynī, Rashīd al-Dīn, Vaṣṣāf — and cannot be corroborated or refuted from the Yuan side. This reinforces the `low` and `medium` confidences set on the `ortoq_equity` liability cells rather than relieving them. Her closing is blunter still: the sources "do not allow us to answer such basic questions as whether or not there were rankings of any sort within ortoy merchant companies," and reconstruction is "hindered on all sides by omissions — both negligent and purposeful." **A second limit now recorded on `CF2`: her Yuan debt material runs the other way.** The 1318 decree on absconding debtors, the 1300 remission of interest for soldiers at Qara Qorum, and Wang Yün's debtors selling their possessions and their wives and children all concern the ortoq's **borrowers**, not the ortoq's liability to his principal. The two directions look alike on the page and must not be conflated. **`FP3` confidence high → medium, with the disagreement recorded as a coding.** Enkhbold has the restricting edicts "frequently ignored"; Endicott-West documents sustained curtailment — commercial tax from 1263 on "people who use government silver to engage in commerce"; household taxes 當差 explicitly not remitted per 1271 decrees quoting Möngke, with exemption available only by registering as a military household; no exemption from state service; weapons surrendered in port from 1293-09-26; and a 1309 memorial rescinding Muslim merchants' imperial letters, tiger tablets and post-horse rights, granted. Privileges then return after Qubilai and especially once the 泉府司 lapses. Kept at `1` because the form confers the arbitrage; **not** coded `P`, since `P` is a structural half-presence and not a value that oscillates over time. **`CF3` confidence medium, tension recorded.** Endicott-West describes the arrangement as one in which investors "pool their capital and entrust it to a manager", and the 泉府司 disbursed the funds of emperor, heir apparent, empress and princes through a single office — which reads multilateral on the investor side. Held at `bilateral` because she is glossing the *commenda* generally at that point and no Yuan instrument survives to settle it. **`ortoq_loan` `CF2`, `LR1`, `AP3` and `LR6` downgraded high → medium, closing the confidence asymmetry between the two regimes.** The asymmetry had tracked Enkhbold's own hedging — his loan-side statements are flat where the equity ones arrive under "it can be hypothesized" — but two limits apply equally to both. Endicott-West's "no commercial contract as such has come down to us in the Chinese sources of the Yüan period" leaves nothing on the Yuan side that could corroborate or refute the Persian narrative either regime rests on. And part of the loan-side claim is inference from the instrument rather than attestation: "A loan, by contrast, makes a borrower's liability unlimited" is a definitional step, not a datum. **No liability cell in either ortoq regime now stands above `medium`.** Thirteen of thirty-five ortoq cells remain `high`, and every one is structural or institutional — `CF1`, `LR2`, `LR3`, `LR4`, `MG1`, `FP1`, `FP3` on the equity side, and `FP4` on the Yuan registration apparatus. **`FP2` added as a deliberate `.NR`.** She describes a virtual state-merchant monopoly over the most valuable maritime goods — at Ch'üan-chou gold, silver, copper, iron and slaves were barred to private merchants — and the pre-Qubilai role of ortoy in tax farming 撲買, which she states did not continue into Qubilai's era. Neither satisfies `FP2` as written, which requires an exclusive right or franchise **held as property**; an exclusionary trade regime run by the state in its own interest is not a franchise the ortoq owns. `.NR` rather than `P` or `0`, because the question in `FP2`'s terms is not one the source asks.
- **`ortoq` split into `ortoq_equity` and `ortoq_loan`, coded on 34 rows** (`OF-0188`–`OF-0221`) in `organizational_forms`, from Enkhbold 2019 (*Central Asian Survey* 38:4, `10.1080/02634937.2019.1652799`). `organizational_forms` 0.6.0 → **0.6.1**: rows and census codes are additive and this repository has reserved the minor for schema and vocabulary changes (`source_lang` enum at 0.2.0, `LR6` at 0.3.0, the dependence columns at 0.4.0, `articulation` at 0.6.0). Codebook regenerated, 187 → 221 rows. **The split is forced, and the tontine row states the principle: a type that spans both values of a characteristic cannot be held in one row.** The ortoq spans both values of *four* at once. Under the early regime (Ögedei, Möngke) the principal bears losses capped at his investment and the agent bears none — Ögedei kept funding an ortoq through repeated failures (Juvaynī 1958, 209–210) and Rashīd al-Dīn 725 has investors unable to recover and trying to compensate their partners. Under the later Yuan and Il-khanid regime ortoqs were "completely liable for the loans they took, which extended beyond partnership capital to their private properties," with guarantors unconditionally bound and, on default, seizure followed by enslavement of the defaulter and his family (Rashīd al-Dīn 1998, 726). Enkhbold dates and explains the shift: the government spent 76,000 *ding* of silver in 1240 bailing out ortoqs, and "the huge losses suffered by Mongol elites motivated Mongol authorities to shift towards a risk-transfer model." Allsen 1989, 118 had already warned that the literature "may have confused two kinds of partnership capital: investment and loan"; the split is that warning made codeable. **The result.** `ortoq_equity` is **identical to `qirād` on all nine comparable cells**, and identical to `commenda` on all nine. `ortoq_loan` differs from `qirād` on five of nine — `AP3`, `CF2`, `LR1`, `LR6` and `LR3` — and differs from `ʿisqa` on only two of thirteen. A single institution therefore occupies both poles of the `agent-loss-exposure` group. **Correction to a first reading of this, made before the periodisation was checked: it is NOT a traversal.** Enkhbold has loan financing "adopted to finance business partnerships when Ögedei and Möngke ruled the Mongol Empire" — contemporaneous with the equity examples, which come from Ögedei's own reign — and only "eventually became prevalent … by the end of the thirteenth century." Equity and loan are **coexisting contract types whose relative prevalence shifted**, not a sequence, and the type rows are now unperiodised to stop the sequence reading returning. The 1240 bailout explains the shift in prevalence, not a change of form. This matters for what the case can be made to bear: two contract types available side by side within one institution is weaker evidence about lability than a documented movement would have been, and stronger evidence that the two configurations are alternatives a single commercial culture can hold at once. **Confidence downgraded on four cells after re-reading, and the reason is recorded rather than smoothed.** Enkhbold's own framing is that "Evidence of the liability borne by the Mongols or their partners is extremely fragmentary"; Lin 2013, 229 holds that it is unclear whether the ortoq had any entity or owner shielding at all, and Enkhbold contends against him; the generalisation is introduced with "Thus, it can be hypothesized that"; and the Fakhr al-Dīn leg rests on **Vaṣṣāf's silence** about the Ṭībī family's obligations. `ortoq_equity LR1` and `AP3` were coded `high` on that basis and are now `low`; `CF2` and `LR6` are now `medium`, since the Ögedei instance is stated flatly and corroborated by Rashīd al-Dīn 725 while the step from instance to form is not. Lin's dissent is recorded in all four notes. `.NR` remains defensible for `LR1` and `AP3` if the hypothesis is judged too thin. **This bears directly on the vault claim that the ortoq is the discriminating instrument, and it sharpens rather than supports the naive version of it.** If one institution can traverse the whole liability axis under fiscal pressure without leaving its own tradition, then a shared configuration is weak evidence of descent: the *ʿisqa* configuration is reachable from the *qirāḍ* configuration by local adaptation, with no contact required. That is direct empirical support for the vault's own diagnostic — *divergent arbitrary particulars*, not functional similarity, is what carries transmission. Note also that `ortoq_equity` cannot be distinguished from `qirād` on any coded cell, which is Galton's problem in its pure form: identity on a functional character set is equally consistent with descent and with independent arrival, and the ortoq had Uighur and Central Asian Muslim merchants in it from the start. The census cannot adjudicate the ortoq's origins and should not be asked to. **`TS2` is `.NR` for both regimes, deliberately.** Enkhbold: "The maximum time of validity of a Mongol–ortoq partnership is not entirely clear." Both readings are attested — the Polos free to leave once Berke's wares were sold, maritime merchants contracted per voyage, but also ortoqs "continuously employed by their Mongol investors, since the duration of these partnerships was at the will of the partners." `single-venture` must not be imported from `commenda`; that inference is what these two rows exist to refuse. Since `TS2` is one of the five cells separating `ʿisqa` from `qirād`, the `.NR` also means the ortoq's position on the duration axis is genuinely open rather than conveniently placed. **Endicott-West 1989 is not in hand and her cells are left `.NR` and named as hers** — `MG4 authorization source` and `FP4 legibility registration` for both regimes. Enkhbold reports through her that penalties for defaulting ortoqs were "confirmed in writing in 1295" and that reforms made ortoqs subject to official oversight and taxation, but oversight is not authorisation and a coding may not exceed a second-hand citation. `FP4` on `ortoq_loan` is the cell most likely to separate the two regimes once she is read. **One comparative-concept complaint, recorded not repaired.** `FP3 identity wrapper` is defined as conferring "legal-status or access arbitrage for its **principals**", and the *paiza* conferred it on the **agent** — relay mounts, provisions, and the tax evasion Enkhbold says edicts failed to stop. The characteristic is written around forms whose beneficiaries are their members; a bilateral capital-labour contract has no such symmetry. The same asymmetry shows in `AP3`, where "members' personal assets" has two referents in a capital-labour contract and they move in opposite directions between the two ortoq regimes. **`organizational_form_type.csv` has no `cooccurs_with` / `cooccurrence_basis` columns and therefore cannot record that these two are not independent observations.** `loss_mitigation_type.csv` gained those columns precisely so that forms drawn from one community could not be counted as separate cases. `ortoq_equity` and `ortoq_loan` are one institution in two contract forms — a tighter dependence than any pair those columns were built for — and the only place that can currently be said is prose in `key_source`, where no check can see it. The asymmetry matters because `organizational_forms` is where the `qirāḍ`/`commenda`/`ʿisqa`/`ortoq` cluster lives, i.e. exactly where an unweighted similarity claim would be tempting and exactly where non-independence would wreck it. Recommended: port both columns across, with `same-institution` added to the basis scale above `same-instrument`, and register them in `check_vocabularies.py`. Not done here — it is a vocabulary schema change and belongs in its own commit. `ortoq` is retained as an uncoded umbrella row and annotated, on the `tontine` precedent. `ortoq_alloc` in `loss_mitigation_type.csv` had its cross-reference updated to point at the two new codes and now records why it stays uncoded there: `LR4=0` in both regimes, so there is no pooling aspect for that census to hold.
- **Four tanomoshi-kō coded on 76 rows** (`LM-0227`–`LM-0302`), from 三浦 1959 (『史林』42-6), 松永 1999 (『史料館研究紀要』30) and 加藤 1998 (doctoral thesis), ch. 5. Nine type rows added, four coded and five parked.

  | code | form | date |
  |---|---|---|
  | `ko_yamakuni_1555` | 二石頼母子, 丹波国山国庄, 親 井ノ本左近 | 1555–1563 |
  | `ko_mitarai_mujin_1773` | 「無尽」, 安芸国御手洗町 | 1773– |
  | `ko_mitarai_gin_1805` | 「銀頼母子」, 安芸国御手洗町 | 1805– |
  | `ko_hokinosawa_1820` | 久左衛門始頼母子, 越後国朴木沢新田 | 1820–1838 |

Parked, not coded: `ko_horyuji_50kan`, `ko_furuichi_sengan`, `ko_meio_1498`, `ko_kosaburo_1802`, `ko_mitarai_hanei_1840`. The three Mitarai associations and the two Nara ones carry `cooccurs_with … same-community` / `uncertain` so that they cannot later be counted as independent cases.

**The result is negative, and that is the entry.** Twenty forms become twenty-four and **no characteristic gains a state**: `CN1` 3→3, `PY2` 3→3, `MC1` 3→3, `PR1` 3→3, `MB1` 4→4. What rises is missingness — `RB3` 2→6, `VF1` 2→6, `MB1` 0→4, `GV1` 5→9. Pairwise on cells where both forms carry a substantive value, `ko_yamakuni_1555` is **identical to `ko_mitarai_gin_1805` on all eight**, and to `ko_mochiyori_yutsu_1876` on all eight; `ko_mitarai_mujin_1773` is identical to `ko_hokinosawa_1820` on all twelve. A rice-denominated kō in a Tanba estate in 1555, reconstructed from the deeds by which its members sold their patrimony, does not separate from a silver kō run by port-town rice merchants in 1805 under a written rulebook. Only four pairs in the whole kō set separate at all, on `MB3`, `CN3` and `PY2`.

Either the kō is one institution across two hundred and fifty years, or the characteristic set is blind to the dimension along which kō differ. The second is better supported, because the three authors differ from each other almost entirely on the **security and enforcement regime**, and `RB3` cannot express any of it.

**`RB3` is the reason, and it is now the census's best-evidenced vocabulary gap.** `allowed_values = vessel|cargo|personal|none` were written around the bottomry/respondentia contrast, as the characteristic's own `definition` says. The pledges in these three sources are: arable land (三浦: 頼母子講維持策として絶えず田地を質として要求している, and specifically land yielding enough 得分 to sustain the 懸足, which makes the pledge a membership qualification rather than a security taken after the fact); town lots, storehouses and outbuildings (竹原屋平三郎's 表口16間、裏行き22間の屋敷と2棟の蔵, registered under the town officers' 裏印); rice in store quantified in bales (2000俵 for 小原屋武七, 500–700俵 for three others); third-party suretyship made mandatory for a stated reason (屹度請合人相立…若懸戻し銀不埒之時者、右請合人ゟ相究可申事, because 右質所売払候迄も、所ニゟ急ニ埒明不申方角も可有之候間 — foreclosure was slow in some jurisdictions, so the pledge alone would not do); and, by 嘉永5年, a member's own future draw (右は今後の自分鬮当金を引当としている). Five `.NR` now stand as the recorded motivation. Not repaired in this cycle: proposing the repair in the same batch as the coding that motivates it is retrospective selection.

Note that `RB3` already separates kō on `none` — `ko_usui_bango_1822` is `high`/`articulated` for `none` on 米谷 2026, against associations that demanded a registered pledge. So the facet is not inert; the positive values are simply absent from its vocabulary.

**`VF1` is now `.NA` on eight of eight kō.** Every one has `PY1=rotation`, so no loss is ever claimed and there is nothing to verify. The characteristic has zero variance across the entire Japanese material and is carrying no information about it. Recorded as a candidate for review, not acted on.

**`MC1` is strained a fourth distinct way.** Coded `pooling`/`medium` for both Mitarai forms with the hedge on the record: 加藤's argument is that these performed a 商業資金調達機能 against the received image of 庶民金融 — 掛銀 of 1000–2000匁, 取銀 above 10貫匁, a third of the members rice merchants, returns of 1–6% he judges unattractive under 安永 and 天保 inflation, so that 一時に多額の資金を調達できるメリット was the point. What is mutualised is trading liquidity, not a peril. Not coded `.NR` only because the 安永2年 仕法書 carries a genuine liquidity-shock clause (御連中之内、不時ニ銀子御入用之儀有之候節者). Fourth form after `particular_average`, `warichi_iwade` and `ko_daikokuya_1848` where `MC1`'s presupposition fails, and the first where it fails because the arrangement is commercial funding rather than mitigation.

**Allocation rule is not a period or regional marker, and `PY2` records that.** 御手洗町 ran lot-allocated and bid-allocated associations simultaneously: the 安永2年 無尽 draws by 鬮 with the premium fixed by rule at 500目 on 1貫目, while the 寛政11年「入札頼母子定法書並連名帳」 and the 天保11年 繁栄持寄講 allocate by 入札 every 4月1日. `ko_mitarai_gin_1805 PY2` is therefore `.NR` rather than filled in from its neighbours. `ko_yamakuni_1555 PY2` is `.NR` for a different reason: 三浦 writes 落札 throughout but his own note has 講親は初回に落札するのが慣例, so the 親 took round one by customary right and the term cannot bear an auction reading.

`ko_hokinosawa_1820` supplies the **control case** for the bidding-kō transfer: order random, price fixed, the 刎金 set at one quarter by the 極証文 rather than bid. Two forms in the census now carry the auction mechanism and two carry its absence.

**Two data the census has no way to hold**, recorded here so they are not lost. 法隆寺's 五十貫頼母子規式 has 縦雖有天下一同徳政、此懸支懸銭不可有改動之儀事 — the association's 懸銭 stands even against a realm-wide 徳政, and 三浦 shows that naming a kō for a religious purpose (極楽頼母子; 経蔵修理用途) was the device for obtaining that exemption. This is a hazard to the **fund**, where `HZ1`/`HZ2` ask about the members' peril. And 加藤's 銀頼母子帳 has 銘々私之取引有之候共、此頼母子ニて差次之儀者、一向相成不申事 — a no-set-off clause barring members' private dealings from the association's accounts, which is asset partition and belongs to WP2 vocabulary rather than WP1's.

**Sources are image-only PDFs.** Every quotation in the `notes` fields was transcribed from rendered page images, not from a text layer. 三浦's Zotero OCR layer is badly degraded — it renders 頼母子 as 頼爆子 and 資支 as 葱支 — and was not used. Check against print before publication. The DOI `10.14989/shirin_42_773` for 三浦 1959 is a Kyoto University repository DOI and was **not resolved through CrossRef** in this session.

Verified before writing: `frictionless validate` VALID on all three datasets; `check_vocabularies.py` clean (6 files, 129 codes); `check_dependence.py` 0 applicability and 0 articulation violations. `views/loss_mitigation_forms--risk-pooling.md` regenerated (24 forms × 9 characteristics).

### Changed

- **`loss_mitigation_forms` datapackage `version` 0.1.0 → 0.2.0, and the minor rather than the patch is deliberate.** Not because 76 rows were added — rows are additive and would have been 0.1.1. Because `RB3`'s `cargo` was **renamed** `goods`, which removes a state a downstream consumer may have been filtering on. At 0.x SemVer puts a breaking change in the minor; at 1.x this would have been 2.0.0. Repo-level release versioning (`CITATION.cff`, the dated `## [x.y.z]` headings below) is a separate space and is untouched — dataset packages advance independently, which is why `organizational_forms` sits at 0.6.0 and `clearing_records` at 0.2.0. Note this bump also carries the earlier unreleased `loss_mitigation_forms` work in this section, which had not moved the version. **CONTRIBUTING §5 has no rule for this case and should acquire one.** It says schema growth is additive — *add a field; do not repurpose an existing one* — and asserts that additive change never breaks a downstream consumer. Both statements are about *fields*. What changed here is a **value** in a controlled vocabulary, which the Table Schema cannot see at all and `frictionless` cannot police; only `check_vocabularies.py` can. Renaming a state is exactly as breaking as repurposing a field and the manual is currently silent on it. Recommended addition: value sets may be extended freely, but renaming or removing a state is a breaking change requiring a minor bump at 0.x and a major at 1.x, plus an explicit note of the old→new mapping. The mapping for this change is `cargo → goods`, one affected row (`respondentia`).
- **`RB3 security or pledge` value set widened, and it is the first characteristic that separates one kō from another.** `vessel|cargo|personal|none` → `vessel|goods|real-estate|surety|personal|mixed|none`. Decided by the maintainer as a separate act from the coding above, on the five `.NR` that coding recorded. **`cargo` renamed `goods`, and no new value added for commodities.** 小原屋武七's 2000俵 of rice pledged at 御手洗 is the same variable in the same state as respondentia's cargo: movables in trade securing an advance. The difference one feels — respondentia's goods are at peril, warehouse rice is not — is `RB4 discharge on total loss`, which exists to carry exactly that. A separate `commodities-for-sale` beside `cargo` would have split Mitarai from respondentia on a distinction imported by the analyst: spurious duplication, not repaired conflation. The bottomry/respondentia contrast the characteristic exists for is unchanged, now `vessel` against `goods`. **`real-estate` added** for land, town lots and storehouses. **`surety` split out of `personal`**, which was silently doing duty both for security on the borrower's own estate and for a third party standing behind him. 加藤 1998, 147 is what makes the split non-optional rather than tidy: the 請合人 is mandatory *because* 右質所売払候迄も、所ニゟ急ニ埒明不申方角も可有之候間 — foreclosure could not be effected quickly everywhere — so property and surety are two facts about enforceability, not two words for one. This is a test-1 conflation repair in Sereno's sense, the only justification `CHARACTER-CODING.md` accepts. **`mixed` added** for arrangements taking both. **Deliberately excluded: the pledge of a claim on the pool.** 松永 1999, 297: 右は今後の自分鬮当金を引当としている — a member pledging his future draw to 国田善徳寺 and 田中村八木家. `RB3` asks what secures an advance *from* the pool; there the pool is the collateral and outsiders are the creditors. Folding both into one characteristic reproduces the `LR2` defect precisely. Pledgeability and transfer of a membership share are transferable-claims vocabulary and belong to `organizational_forms`. **Recodings.** `respondentia` `cargo`→`goods`. `ko_mitarai_mujin_1773` `.NR`→`real-estate` (high, articulated): 鬮当リ之節者、当所連中之内慥成質物書入ニて、所御役人中之裏印を取 — a mortgage countersigned by the town officers. `ko_mitarai_gin_1805` `.NR`→`mixed` (high, articulated): mortgage deed plus mandatory 受合証文, over a 質所 spanning real property and rice in store, with verification zoned by place. **Two cells stay `.NR` although a fitting value now exists, and that is the discipline.** `ko_yamakuni_1555`: 三浦's 絶えず田地を質として要求している is a general finding about medieval kō, not an attestation for this association, whose surviving evidence is land *sales* by exiting members — default running its course, not a pledge clause. `ko_hokinosawa_1820`: the 極証文 records no security. Widening a vocabulary does not license filling a cell the source never filled. **Result.** Of 54 kō pairs with at least one comparable cell, those identical on every comparable cell fall **38 → 36**; pairs that separate rise **16 → 18**. Both new separations are `RB3` alone: `ko_mitarai_mujin_1773` vs `ko_usui_bango_1822` (12 compared, differ on `RB3` — a registered real-property mortgage against an articulated `none`), and `ko_mitarai_mujin_1773` vs `ko_mitarai_gin_1805` (11 compared, differ on `RB3` — property alone against property plus mandatory surety, in the same town thirty-two years apart). **Before this change no two kō in the census separated on any positive value; the whole set collapsed into one signature.** The facet was never inert — `ko_usui_bango_1822 RB3=none` was already `high`/`articulated` on 米谷 2026 — its vocabulary simply had no positive state a kō could take. **Two open obligations, recorded so they are not quietly dropped.** `real-estate` is at present taken only by Japanese forms, and a value one tradition monopolises labels the tradition rather than discriminating within the set; it earns its place when a non-Japanese form takes it, with `confraternity_fund`, `avariz_fund` and the *waqf* corpus the candidates. `surety` and `personal` currently have **zero occurrences each**, so the split is so far a hypothesis about where the cut belongs; the first form that should test it is 松永's 田中村徳蔵寺頼母子, where the 小作農 長蔵 of 頸城郡中山村 participated on 親方・地主の保証 with no land of his own to pledge. And `mixed` on `ko_mitarai_gin_1805` is carrying two mixtures at once — several kinds of property, and property together with surety — which that row's note flags as the evidence should the distinction ever need splitting. Verified: `frictionless validate` VALID, `check_vocabularies.py` clean (the `allowed_values` sweep is what would have caught a missed `cargo`), `check_dependence.py` 0 violations, view regenerated.

### Fixed

- **`build_views.py --check` reports the risk-pooling view stale under its own defaults, independently of any data change.** The committed view header reads `Mechanism filter: MC1 = None`, i.e. it was generated with `--mechanism all`; bare `--check` defaults to `--mechanism pooling` and therefore compares against a different rendering. `--component risk-pooling --mechanism all --check` reports it current. Pre-existing and not introduced by this cycle; the fix is to make the default and the committed artefact agree, or to have `--check` read the filter from the file's own header. Not acted on here.
- **`ko_daikokuya_1848` coded on 13 characteristics** (`LM-0214`–`LM-0226`), from 小林惟司 1986 (『三田学会雑誌』79-3) **read in full**. The 大黒屋善兵衛頼母子講 of 長興寺村, 摂津国豊島郡 — the census's first 領主的金融講. Two parked type rows added alongside it, `ko_gotensama_1843` and `ko_choko_1854`, the other two associations 小林 documents in the same village. **`MC1=.NR`, and that is the finding.** 小林 shows the association was 強制的に組織して返済を期待せず直接藩財政に入れてしまった and calls it 献金講 — a donation kō. A kō in form and a levy in function: nothing is mitigated and no output reaches members, so `PY0` is `.NR` too. Third form whose `MC1` cannot be set, after `warichi_iwade` and `particular_average`. **`CN2=.NR` records a vocabulary gap with a substantive finding behind it.** Contributions are a two-part tariff — 高割 proportional to holding *plus* 家割 flat per household — and `CN2` admits one basis. The interaction is where the regressivity lives: the landless paid no 高掛り but 無高上層 were charged **1分 to 2分2厘 more flat charge than 高持百姓**, with a 無高下 stratum below. 小林: 石高が多いほど実質的な負担は少なくなり、無高ほど実質的な負担が多くなる。すなわち累進ではなくて逆進. This is a **fourth mechanism** by which a formally pooling institution disperses — after survivorship (tontine), emergent auction (bidding kō) and exemption (*hikikuji*) — and it sits in the arithmetic of contribution, before any allocation occurs. Confidence is capped throughout: 小林 states that no 議定 or 仕法書 survives for this kō, only the 銀集帳, so procedural cells are inferred rather than read from a clause. Two further data recorded in the rows: 高槻藩 from 文久2 ran a kō with **an ex-ante model calculation of profitability** at 25,000 shares, which would code `PR1` differently; and 岡部藩's compulsory enrolment order of 寛政3 was **refused** by the villages, so compulsion was resistible.
- **`cooccurs_with` reference validation** in `check_vocabularies.py`. Every code listed must exist, cross-dataset references resolve through `code@dataset`, self-reference is an error, and a `cooccurs_with` without a declared `cooccurrence_basis` fails. Added immediately after a dangling pair was written into `ko_daikokuya_1848` — the earlier referential check covered `data.csv` and not the vocabulary's own new column.
- **`scripts/check_vocabularies.py`, wired into CI ahead of the frictionless step.** Standard library only, so CI gains no dependency. Non-zero exit on any failure. Three groups of checks: **Structure** — ragged rows, header integrity, duplicate or empty codes, leading/trailing whitespace, BOM and CRLF, per vocabulary file. **Referential** — every `type_id` and `char_id` used in a dataset resolves to a code in the matching vocabulary, and **every `value` falls within that characteristic's `allowed_values`**. **Agreement** — where a field is deliberately carried in both a `datapackage.json` enum and a vocabulary, the two list the same set. The `allowed_values` sweep closes a gap CONTRIBUTING §5 previously left to manual checking, and it is the one that matters most: `value` is a single column holding a different permitted set for every `char_id`, and **a Table Schema cannot express a conditional constraint at all**. Frictionless was never going to catch `MC1 = mutualising` or `PR1 = priced`; it now fails in CI. Tested against the defects that actually shipped and against injected ones. Reintroducing the unquoted comma in `warichi_iwade` fails at the ragged-row check, and the cascade is instructive — one malformed row silently drops the form code, which then fails eighteen downstream `type_id` references. Out-of-vocabulary values, ternary characteristics given nominal values, duplicate codes, padded fields, dangling `char_id`s and an enum/vocabulary divergence are all caught. **The division of labour is now stated as a rule in CONTRIBUTING §4** rather than as a standing hazard: conditional constraints and growing censuses belong to the vocabulary; small closed schematic sets belong to the enum; the two fields carried in both (`instrument_type`, `amount_unit`) are duplicated on purpose, so that an outside consumer running plain `frictionless validate` on the published deposit gets the same enforcement we do, with the agreement check preventing drift. New duplicated pairs must be registered in `ENUM_VOCAB`. `type_id` and `char_id` remain deliberately un-enum-constrained: the census is growing, and enumerating them would mean editing two files per form. The referential check is the right enforcement for an open list. The script **reads each dataset's declared `missingValues` rather than restating them**, so it cannot drift from the schema it checks against. An earlier draft hardcoded the token set and would have needed an agreement check to stay honest; deriving removes both the duplicate and the check. Verified by adding a token to a schema and a row using it — tolerated — while a genuinely out-of-vocabulary value in the same file still fails.

### Changed

- **`tontine_fr_royal PR1` corrected from `1` to `P`**, `confidence` medium → high, on Coudy 1957 (*Revue historique de droit français et étranger* 35), now a pinned source rather than a web survey. **This was the cell the "pricing is orthogonal to mechanism" result rested on, and verifying it improved the result.** The fourteen classes are confirmed exactly — 1689, five-year bands, first to age five accomplished, last from sixty-five; a fifteenth from seventy in 1696; twenty in 1709 — and the rates genuinely were graded: *l'intérêt sera déclaré progressif, en 1689 : du denier 20 au denier 8 dans les treizième et quatorzième classes*, five per cent rising to twelve and a half. But the grading is **not actuarial**. Coudy gives its purpose as *dans un but d'équité* and states that *l'on ne disposait d'aucune table de mortalité. L'on ne s'en souciait guère non plus*; he doubts the Crown grasped the operation's real cost. Peril priced ex ante by age band and not from any distribution is `P`, on the reading already applied to `sea_loan`. **The census is better for it.** `PR1` now runs `0` (`tontine_en_1693`, undifferentiated), `P` (`tontine_fr_royal`, graded without a table) and `1` (`widows_fund_scotland`, built on projected mortality) **inside `MC1=pooling`** — the hazard-to-risk gradient as three coded values in one mechanism, all coexisting, rather than a binary asserted as a sequence. One further datum, recorded in the row: **Deparcieux (1746) derived the survival chances of each class by observing the 1689 and 1696 tontines.** The actuarial knowledge came out of these instruments, not into them.

### Fixed

- **Two ragged vocabulary rows**, each caused by an unquoted comma inside a field, which shifted every value after it. `loss_mitigation_type.csv` (`warichi_iwade`, introduced in this cycle) and `amount_unit.csv` (`ryo`, pre-existing and unrelated). Both repaired and both files rewritten with proper quoting. **Neither could have been caught by CI: the vocabularies are not declared frictionless resources.** Three datasets are validated on every push; the six vocabulary files that carry `allowed_values`, component assignments and the dependence columns those datasets are checked *against* are not validated at all. A ragged-row and header check over `vocabularies/*.csv` is a few lines with no new dependency and is recommended — see logbook 6.

### Added

- **`cooccurs_with` and `cooccurrence_basis` columns on `vocabularies/loss_mitigation_type.csv`.** Records which forms are attested together and — separately — *at what level*, so that the independence of forms can be assessed rather than assumed. Motivated by the observation that a single community may run several pooling arrangements segregated by medium (land, labour, money), in which case forms coded singly are not independent observations, and counting them as separate cases inflates *n* the way four collinear characteristics inflated the *ʿisqa* evidence. `cooccurrence_basis` runs tightest to loosest: `same-instrument`, `same-persons`, `same-venture`, `same-community`, `same-polity`, `uncertain`. Twelve of thirty-two rows populated. Strongest cases: `tontine_en_1693`/`life_annuity_priced`, offered under the same Million Act; and `ko_usui_bango_1822`/`ko_daimanin_1828`, whose 仕法 were drafted by the same 銀預り. **Nothing in `data.csv` changes and no comparative computation uses these columns.** They constrain what may be claimed, not what is recorded — description is unaffected, similarity and configurational claims are not. Two caveats recorded in logbook 6: the cross-dataset reference syntax `code@dataset` is invented and wants ratification; and `same-venture` as applied to the maritime cluster records structural possibility rather than an attested joint instance, so the scale currently mixes the two. **And the case that motivated the column — `warichi_iwade` — is `.NR`**, because the Echigo kō it would pair with were never coded.
- **`particular_average` coded on 18 characteristics** (`LM-0196`–`LM-0213`) — **the census's first non-sharing baseline**. Not a mitigation institution but the default rule against which Harris's three mechanisms are defined: accidental damage "from an irresistible or unforeseeable external cause" falls on the owner under *casum sentit dominum* (Addobbati 2023). Added because a census containing only sharing arrangements has no null, and a criterion with no null is unfalsifiable. **`MC1=.NR` is deliberate and is the finding.** Allocation, spreading and pooling are three ways of moving a loss; particular average moves it nowhere. It is not a fourth mechanism but the absence of one, and coding `allocation` would be wrong — that value means risk assigned to a named party *by contract*, whereas this operates in the absence of any arrangement. **`RB1=capital-provider` against `general_average RB1=all-stakeholders` is the discriminating pair**, and the contrast is exact: same voyage, same hull, same peril, with incidence turning entirely on whether the damage was deliberate. Thirteen cells code `.NA` — no membership, no contribution, no fund, no duration — which is itself informative: the vocabulary is built for *arrangements*, and this is not one. **`RB3` records a contractual override.** Zanini shows that under bottomry, particular-average damage reduced the borrower's repayment proportionally "since the relative risk had been transferred to the lender." The default rule is displaceable, and `sea_loan` is one instrument that displaces it. **`VF1=.NR` flags a gap.** No verification is needed *within* the institution, since nothing is shared — but the classification requires it, Dreijer noting the distinction mattered "particularly for insurers." Verification attaches to the boundary rather than to the loss, and `VF1` cannot say so. Context: Harris 2023, from whom `general_average` is coded, uses "particular average" not once, though the category is standard across the Fusaro volume. Recorded in logbook 6 as a question about what the `general_average` row denotes.
- **`warichi_iwade` (割地) coded on 18 characteristics** (`LM-0178`–`LM-0195`), from Brown 2006 (`10.1215/01455532-2006-004`) and Brown 2011. Periodic reallocation of arable by drawn lot in Iwade village, Echigo — the first land-based form in this census. **`MC1`, `HZ1` and `HZ2` are deliberately `.NR`.** Reading *warichi* as spreading flood impact is the **received view in the Japanese literature** (Furushima 1939; Aono 1982), from individual village studies. Brown's regional GIS test over 113 villages finds flood and landslide frequency "insufficient causes by themselves, even if scholars ultimately see them as necessary conditions." Coding `pooling` would enter a contested orthodoxy as data; coding a hazard correlation would assert what its only regional test failed to establish. **`CN1=.NA` is the informative cell.** *Warichi* has no contribution stream and no fund — it reallocates an existing asset rather than assembling one, where every other form in the census collects something. `CN2`, `CN3` and `LS2` propagate `.NA`. That single fact is why the pooling category fits badly, and it is now visible in the matrix rather than in prose. **`PY2=rotation-lot`** is the best-attested cell (*kujikae chō* lot-exchange notebooks; nine *kujigumi* lot-drawing groups across sixteen shares in 1756–57).

### Proposed, not implemented

- **No characteristic represents an opt-out from the allocation mechanism.** *Hikikuji*, the right to withhold land from redistribution, ran from under 2 per cent of Iwade's land in 1756–57 to 14 per cent in 1818, exceeded the 5 per cent Kaga permitted its shareholders, and was "concentrated in a few hands, not universally shared." This is structurally distinct from the channels already recorded in the tontine and the bidding kō, where the transfer toward the strong is *constitutive* of the mechanism rather than an exemption from it. Recorded in logbook 6.
- **`PY0`/`PY1` presuppose a queue.** Both are coded for *warichi* on the basis that a draw occurs irrespective of loss, but all members draw simultaneously at each redistribution — there is no turn. Every previously coded rotating form allocates to one member per round.
- **`torinoke_mujin` (取退き無尽) coded on 14 characteristics** (`LM-0164`–`LM-0177`), from 並松 2022 and 中川ほか 2013. The variant in which **a member who wins is released from all further contribution** — 次回以降の掛戻義務から免れ — while the association continues on the 未当籤講員, the winner taking only 講金の一部 so that the retained remainder funds the continuation (`PY3=1`). In every other coded association the winner keeps paying 空掛 to 満会. **`MB1=contract` separates it from the village forms**: 並松 records that the 講元 recruited 血縁・地縁にまったく関係なく, with no kin or locality tie whatever, against 松永's village kō built on 親戚・知人・取引相手・信仰. **`PY2=rotation-lot`**, and the consequence matters: allocation by 抽籤 removes the sorting by urgency that operates in bidding kō, replacing it with 射倖性 — an early draw yields far more relative to contributions than a late one. **Suppressed**: 大塩平八郎 investigated these from 文政12 and reported them to the 老中 in 天保元年 as 不正な無尽, on the winner's exemption and on 講元 returns exceeding the shogunal interest ceiling. That is the census's first documented prohibition.

### Proposed, not implemented

- **`PY2` is under-specified for bidding.** 並松 distinguishes three bid forms: on the member's take (lowest wins), on the difference between total contributions and receipt, and on the **premium or interest rate** (highest payer wins). The third prices the money directly rather than discounting the pot, and is a different object. A split would be a well-formedness repair, but no coded form yet separates on it — test 4 territory, and it should wait for a form.
- **No characteristic records the duration of a member's contribution obligation.** `CN1` gives timing and is silent on whether a winner keeps paying. That is the defining feature of `torinoke_mujin` and arguably means `CN1` asks two questions.
- **External regulation has nowhere to sit.** Prefectural **floors on the winning bid** — 50 per cent of the payout in 鹿児島, 60 in 長野・三重, 70 in seven prefectures, 80 in 栃木 — are ceilings on the implicit interest, aimed at the transfer from the liquidity-constrained to the liquid. With duration caps, scale caps, 世話人 minimums and kin/workplace exemptions, this is a substantial body of evidence describing constraints imposed on a form from outside, where every existing characteristic describes the arrangement's own terms.
- **`nakai_fictive_household` coded across all 32 characteristics** (`OF-0156`–`OF-0187`) — the first Japanese form in `organizational_forms`, from D'Amico 2026 (*Enterprise & Society*, `10.1017/eso.2026.10131`) on the Nakai Genzaemon archive. Tokugawa merchants exploited the fact that an *ie*'s name need not correspond to a real person, building fictive households that held assets, contracted and were registered as households with nobody behind them; Nakai Iwanosuke "founded" the Osaka store in 1806 aged five, died in 1807, and kept paying interest on the main house's prospective funds until 1845. **`MG4=private-contract` and `CI3=0` are the load-bearing cells.** No charter and no grant: the authorities enforced private contracts but designated partnership accounts *nakama-goto* and non-adjudicable, so the form arose **against a standing legal refusal** rather than in the absence of enabling law. And transferability never developed — D'Amico: Tokugawa houses "failed to develop joint-stock equity finance or transferability in the absence of any real use for these features." Shares (*kuchi*) were proportioned but did not circulate. **`MG4` now has all four values occupied** (state-charter, customary, religious, private-contract). **`FP3 identity wrapper` gains its first `1`** — every other coded form is `0`. **`AP1=1` with `AP3=0`** gives entity shielding without limited liability in one form, the Hansmann–Kraakman asymmetry observed outside the European lineage and consistent with Harris 2020 on the late plurality of limited liability. **Unexpected near-neighbour: `waqf_khayri`.** On the WP2 five the two nearly coincide — `AP1=1/1`, `TS1=1/1`, `CI3=0/0`, `CI2=1/P` — diverging on `LP1` (0/P), `MG4` (religious/private-contract) and `FP3` (0/1). A Japanese commercial network and an Islamic pious endowment reaching the same entity-continuity-without-transferable-claims profile from unrelated ancestral conditions and opposite purposes. Flagged as a candidate independent-arrival pair to be assessed, **not** asserted. Coded conservatively where the source refuses: `LP3=.NR` (D'Amico: bankruptcy liability for these entities "remains a mystery"), `LR1` at `low` on the single 1839 Karasu dissolution, `AP4=P` at `low` with the waqf-shaped caveat live. `MG3=.NA` — `MG1=contract` leaves no membership to restrict, caught by `check_dependence.py` after an initial `.NR`.
- **Five Japanese rotating associations coded at association level** (`LM-0104`–`LM-0163`, 60 rows): `ko_mochiyori_yutsu_1876` (持寄融通社, 池田 2022), `ko_hokoin_1832` (宝光院's kō, 田中 2011), `ko_usui_bango_1822`, `ko_shijunin_1829`, `ko_daimanin_1828` (all 米谷 2026). **The unit is the named association backed by a surviving 規約書 or 仕法帳, not an ideal type** — the parked `ko_mujin` row is superseded and left uncoded. **Why the ideal type was abandoned, which is the finding.** 碓井村伴吾講 allocates 初会入札、弐会目振鬮、終リ迄一会替リ — bid and lot alternating round by round to the twenty-fifth. 四拾人講 allocates 振鬮 alone across forty-one rounds. 大満院講 inherits 伴吾講's rules verbatim because 岡田伊左衛門 and 塩野久右衛門 drafted the 仕法 for both. Same district, seven years apart, overlapping drafters, different rules. **Allocation is a drafting choice by named 銀預り, not a property of the tradition**, so no sentence of the form "Japanese rotating associations allocate by lot" or "by bid" is supportable. Period splitting was rejected on independent evidence: 松永 1999 shows collateral entering the same village form between Kyōhō and Hōreki–Meiwa driven by widening geographic scope and consequent 信用が不安, not by date. **Cells that gained variance.** `CN3` takes its first `P` — 伴吾講 enforces only by forfeiture of that round's draw (掛銀不参 voids even a submitted 入札), against the 持寄融通社's litigation and forced sale of pledged land under the 規約書. `LS2` now runs `1`/`P`/`0`/`.NA`: 伴吾講 is `1` because the five members who never drew receive 1貫760匁 each at the 弐拾六会, where a tontine's non-survivors receive nothing. `MB1` holds `confession` (社寺講, joined as 檀那 out of 信仰心), `contract`, and `territorial-subject`. `PY2` holds `auction`, `rotation-lot`, `indemnity` and three `.NR`. **A discriminant-validity result on `MC1`.** All five associations pass Harris's pooling test and are functionally three different objects. 持寄融通社 was convened for one man's relief from debt and he took the full 2,300円 in round one. For 社寺講, 加藤's finding (via 田中) is that the 名望家的商人 subscribing did not consider investment return and were in NPV terms lenders to the 講元 — donation wearing a pooling form, as the tontine is dispersal wearing one. `MC1=pooling` is satisfied by mutualisation, by subsidy and by dispersal alike. Reported, not repaired. All rows `coder=ai` and provisional; `source_lang=ja` throughout. 榊原 2014 deliberately **not** coded — it is a three-person three-period model, and coding structural characteristics from it would be coding a model rather than an institution.

### Proposed, not implemented

- **A token or field distinguishing "no fitting value in the vocabulary" from `.NR` "not recorded in the source".** Several cells in this batch carry `.NR` for the former reason — `PY2` where allocation alternates by round, `GV1` where the 講元/銀預り split has no cell, `RB3` where a 田畑書入証 has none. The distinction survives only in `notes`, which is the class of conflation the `.NR`/`.NA`/`0` split exists to prevent. Recorded in logbook 6 for decision.
- **`RB3` fails asymmetrically.** 伴吾講 codes `none` cleanly and articulated (但シ、証文等入不申候); a mortgage over fields and house plots has no cell, because the allowed values are maritime. The vocabulary can express the absence of security but not this kind of presence — the `AP4` comparative-concept problem in a second facet.
- **`tontine` split into `tontine_en_1693` and `tontine_fr_royal`; `widows_fund_scotland` and `life_annuity_priced` added** (`LM-0079`–`LM-0103`, 25 rows). **The falsification test declared against the generic `tontine` row is now runnable, and the sequence it was built to test collapses.** The French royal series from 1689 divided subscribers into fourteen age classes carrying differentiated rates (`PR1=1`); the English tontine under the Million Act of 1693 did not differentiate by nominee age at all (`PR1=0`). Same mechanism, same decade, two polities. **`PR1` therefore has variance *inside* `MC1=pooling` at a fixed date, so ex-ante pricing is orthogonal to mitigation mechanism rather than a stage of it** — and Harris's timing argument survives only as a claim about what a mechanism *requires*, not about when societies learned to price. The generic row could not hold this, exactly as the doubt recorded against it anticipated. The result arrives by the route `CHARACTER-CODING.md` prescribes — **add forms, not features** — repeating the *asiento* pattern in which one new form settled what no amount of reasoning could. **`widows_fund_scotland` is the necessary comparator**, without which `tontine_fr_royal PR1=1` asserts nothing: contemporaneous, also `MC1=pooling`, also priced ex ante, but `PY0=individual-indemnity` against the tontine's `individual-draw`. Pricing and payout direction vary independently across the two — the discriminant-validity separation that `CHARACTER-CODING.md` test 4 says cannot be settled analytically and requires cases that come apart. **`PY0` now carries the pooling census's sharpest distinction on three forms**: `collective-good` (`averia_pool`), `individual-indemnity` (`general_average`, `widows_fund_scotland`), `individual-draw` (both tontines). The tontine's draw corresponds to no adverse realisation of the recipient's — it is funded by other members' extinction — which is why a pooling form can be variance-*amplifying*. Note that it codes `HZ1=idiosyncratic`, so it passes the correlation test `averia_pool` fails and is dispersive anyway: correlation and redistribution direction are independent failure modes. **`life_annuity_priced`** (`MC1=spreading`) records where the quantification episode actually sits — de Witt 1671 against Holland's flat fourteen years' purchase, Hudde's Amsterdam registers, Halley 1693 — i.e. in a different mechanism from the one the tontine instantiates. `LM-0077`/`LM-0078` are **superseded but retained**, per this repo's rule that discarded alternatives stay auditable; retirement is a maintainer decision at review. All 25 rows are `coder=ai` and provisional. **The two load-bearing figures — £108,100 subscribed to the tontine against £118,506 in single-life annuities at fourteen years' purchase, and the French fourteen-class structure — are from secondary literature surveyed 2026-08-07, not from Exchequer records.** Verify against Weir (1989), Velde & Weir (1992) and Milevsky (2015) before analytical use.

### Proposed, not implemented

- **Split `PY1 payout trigger` to carry a locator.** Its allowed values name the *kind* of triggering event and are silent on *whose*: the tontine's trigger is another subscriber's death, so `LM-0082` reads identically to `LM-0097` on `widows_fund_scotland`, where the trigger is the member's own. Two forms collide in one cell on precisely the feature the ergodicity argument turns on. A Sereno well-formedness failure of the kind `CHARACTER-CODING.md` test 1 makes mandatory to repair, with `PY0` and `LR6` as the precedents for splitting rather than extending allowed values. **Deliberately not implemented in the same commit as the result it explains** — the component set must be declared before the coding it is used on, and doing both at once would be the retrospective selection the falsifiability commitment forbids. Goes to review on test 1, evidenced by `LM-0082` and `LM-0097`. `PY2` has no value expressing survivorship accrual either (`LM-0083` records `.NR`); settle both in one review.
- `isqa` to `vocabularies/organizational_form_type.csv`, and a full 31-characteristic coding of it (`OF-0037`–`OF-0067`). The Jewish investment partnership of BT *Bava Metzia* 104b, whose *pelga milveh u-pelga pikkadon* (half loan, half bailment) structure makes it the **unshielded** member of the Mediterranean partnership triad: `CF2` (agent bears capital loss) is `1` and `AP3` (owner shielding) is `0`, inverting both cells relative to *qirād* and *commenda*. `CI1` codes `several-accounts`, which the bifurcated stake fits better than any other form yet in the matrix.
- Contrast codings for `qirad` (`OF-0068`–`OF-0077`) and `commenda` (`OF-0078`–`OF-0087`) across the contractual-form and liability facets. Added because a discriminating cell is unreadable without its comparators: `CF2 = 1` for the *ʿisqa* asserts nothing unless the forms it is being contrasted with are coded on the same characteristic. These rows exist to make the contrast legible in the data rather than only in prose.
- `LR6 coupling symmetry` to `vocabularies/organizational_form_characteristic.csv` (`symmetric|upside-only|downside-only`), coded for `isqa`, `qirad`, `commenda` and `waqf_khayri` (`OF-0088`–`OF-0091`). **`LR2` records whether the decision-maker is bound to outcomes; `LR6` records which way.** The gap surfaced during the *ʿisqa* coding: `LR2` returned `coupled` for all three partnership forms, flattening the difference between an agent holding a call option bounded below at zero (*qirād*, *commenda*) and one holding a debt that can carry him past it (*ʿisqa*). The distinction survived only by accident, in `CF2`. Since asymmetric exposure is what the project's ergodicity argument turns on, a feature space that cannot express it was under-specified rather than merely incomplete.
- **New dataset `loss_mitigation_forms` (v0.1.0)** — 78 rows, 7 forms coded, 25 characteristics, 20 in the census. Covers arrangements that mitigate loss **without an entity**, sorted by `MC1 mitigation mechanism` into Harris's (2023) three-way distinction: **allocation** (risk assigned to a named party by contract), **spreading** (transferred to third parties for a premium), **pooling** (mutualised among stakeholders, no outsider). The scope follows Harris's own closing sentence: "Risk can be mitigated in different ways: allocation, spreading or pooling. We are only beginning to understand under which legal and organizational frameworks and maritime risk environments were risk spread, as opposed to pooled or allocated." `MC1` exists to make that question askable of the data. Forms coded from Harris 2023: `sea_loan`, `bottomry`, `respondentia`, `marine_insurance`, `general_average`, plus `averia_pool` extended. **`RB3 security or pledge` is the single cell separating bottomry (`vessel`) from respondentia (`cargo`)** — otherwise identical. **`RB1`/`RB2` are separate characteristics because the sea loan splits them**: peril risk to the lender, market risk to the borrower, which is Harris's central observation about the form. **`PR1 risk priced ex ante` carries Harris's timing argument** and now reads off the matrix: `general_average`/`averia_pool` = `0`, `sea_loan` = `P`, `bottomry`/`respondentia`/`marine_insurance` = `1`. Pooling needs no ex-ante price because all calculation is ex post, which is why it is ancient; a premium requires probabilities, which is why insurance waits until c.1300; the sea loan prices crudely (25–33% western Mediterranean, 40–100% Levant) and sits between. Knight's threshold as three coded cells rather than a narrative. **The two pooling forms are separated by `PY0`**: `averia_pool` = `collective-good` (the levy bought an armed escort, nobody drew on it), `general_average` = `individual-indemnity` (the owner of jettisoned cargo is made good by the rest). And by `CN1`: `ex-ante-periodic` against `ex-post-assessment`. The facet earns its keep on two forms.
- `tontine` entered as the **declared falsification test** for the hazard-to-pricing sequence, and deliberately left uncoded on `PR1`. If pooling forms should code `PR1=0`, the tontine is the counter-example: mutualised mortality risk with subscribers grouped into age classes, i.e. ex-ante actuarial pricing *inside* a pooling form. Were it to code `PR1=1`, pricing is orthogonal to mechanism rather than a stage of it, and Harris's timing argument needs qualifying. Not coded because (i) no source is to hand and coding it from general knowledge would breach this repo's own rule, and (ii) "tontine" probably spans pre- and post-actuarial forms, so one row may not hold it. Recorded as a test to run, not a result.

### Considered and rejected

- **`cooperative_pooling_forms`, created and superseded the same day.** It scoped the corpus to hazard-mutualising arrangements only. Reading Harris 2023 showed that boundary cuts across his three mechanisms and excludes the comparison he says is unresolved, so the package was widened rather than kept. Every cell, characteristic and form was verified present in `loss_mitigation_forms` before removal; `MC1='pooling'` recovers the superseded corpus as a query. Recorded because the discarded alternative should stay auditable.
- **Splitting the WP1 facet of `organizational_forms` into the new package.** Rejected: it would put one form's cells in two places, create two sources of truth for `isqa LR3`, and turn the WP1-versus-WP2 differential prediction into a cross-package join. The rule adopted instead is that **a characteristic lives in exactly one dataset**; forms suffixed `_alloc` are cross-references coded here only on loss-allocation characteristics.
- **Coding `commenda_alloc`, `qirad_alloc`, `isqa_alloc`, `ortoq_alloc`.** Entered in the census but left uncoded, because Harris cautions against treating the *commenda* as a risk instrument at all: "dealing with risks is not its main purpose... it was also, even primarily, an employment or agency contract." Coding them would decide against that caution; flagged for the maintainer.
- **Superseded: `cooperative_pooling_forms` (v0.1.0)**, with `vocabularies/cooperative_pooling_type.csv` (10 forms) and `vocabularies/cooperative_pooling_characteristic.csv` (19 characteristics). Covers arrangements whose primary function is **mutualising a hazard across a membership** — rotating credit associations, general average, communal levies, confraternity funds, tontines — as distinct from the capital-holding vehicles in `organizational_forms`. **The split is by corpus, not by facet, and the distinction matters.** Coding the same forms' WP1 cells in a second package would have put one form's cells in two places, created two sources of truth for `isqa LR3`, and turned the WP1-versus-WP2 differential prediction into a cross-package join. Instead each package has its own characteristics — a *kō* would code `LR4=1` and nothing else under the old scheme, losing contribution schedule, allocation rule, default remedy and hazard correlation — while **both carry the same `component`/`work_package` vocabulary**, so WP1's four components are the shared analytical layer and the packages join at component level. Boundary rule: `organizational_forms` codes the vehicle, `cooperative_pooling_forms` codes the pool. Where an institution is both, the *units differ* and both are coded: `asiento_averia` (the contracting company, with its *partícipes*) stays in `organizational_forms`; `averia_pool` (the compulsory levy, with its shippers) is coded here. Their `LS3=0` and `AP3=1` describe two different populations shielded in two different ways, which is a finding rather than a duplication.
- `ko_mujin`, `huiguan`, `gongsi` and `huf` **moved** from `organizational_form_type.csv` to the new vocabulary. No data rows referenced them, so the move was free; after coding it would not have been. `huf` carries an explicit boundary doubt — a joint Hindu family is arguably a capital-vehicle — recorded in its `key_source` field rather than silently resolved.
- `PY0 pool output` (`collective-good|individual-indemnity|individual-draw`), **added because the first coded form broke the scheme.** `PY1 payout trigger` silently presupposed that an individual draws on the fund; the *avería* bought an armed escort, a non-excludable good on which no shipper ever drew. That is a well-formedness failure in Sereno's sense — a characteristic asking a question that presupposes an unasked one — so splitting was mandatory, not an embellishment. `PY1`–`PY3` are now gated on `PY0`. The episode is the argument for coding forms early: nineteen rows of one form falsified the payout facet within an hour of its being written.
- `scripts/check_dependence.py` generalised to any registered dataset, with a per-dataset vocabulary map and blocking rules for the new gates. It caught the `PY1`/`PY2` inconsistency above rather than my noticing it.
- `articulation` field on `organizational_forms` (v0.6.0), recording per cell whether the coded function was **articulated by the tradition's own advocates in their own idiom** (`articulated`) or is visible only under the analyst's description (`analyst-imposed`), with `.NR` where not yet assessed and `.NA` propagated with the value. This implements a methodological safeguard the application already promises — that functional coding be bounded by the sources' vocabulary — which until now lived only informally in `notes`. It is what makes *al-ghunm bi'l-ghurm* or *sekharo ke-poʿel batel* evidence rather than illustration. **Populating it caught an error in the first pass.** Twenty-two cells were initially marked `articulated`, six of which rested only on Udovitch — a modern secondary work. But `articulated` asserts that *the tradition said it*, not that a scholar reports it. `isqa AP3`, `isqa LR1`, `qirad AP3`, `qirad CF2`, `qirad LR3` and `commenda CF2` were downgraded to `.NR`. The sixteen retained each name a primary text (BT *Bava Metzia* 104b), a doctrinal term in the tradition's own vocabulary (*taʿbīd*, *shurūṭ al-wāqif*), or a secondary work quoting the clause verbatim (Hierro Anibarro on *"no han de estar obligados en mancomun"*). Current distribution: 16 `articulated`, 17 `.NA`, 122 `.NR`. **Recoverable:** the *qirāḍ* downgrades should return as `articulated` once re-coded with *al-ghunm bi'l-ghurm* as `source_ref` — the maxim is the tradition articulating loss-coupling in its own idiom, and would make `qirad` the worked demonstration that the safeguard operates.
- `check_articulation` in `scripts/check_dependence.py`, enforcing the weakest form of the rule: `articulated` cannot sit beside a `[verify]` citation, since a placeholder cannot evidence what a tradition said. Non-zero exit on violation, alongside the applicability check.

### Considered and rejected

- **Adding `articulation` to `clearing_records`.** Rejected: that dataset records transactions — dates, instruments, amounts, counterparties — not functional attributions, so the question "did the actors articulate this function?" does not arise for a coding that says a document is a *tegata*. Note also that `clearing_records` already implements the descriptive-category/comparative-concept distinction **by field design**, keeping `instrument_wareki` (the tradition's own term) beside `instrument_type` (the analyst's category). `organizational_forms` needed an explicit column precisely because its coding is functional attribution rather than term translation. The Methodology's phrasing — "the *functional* coding is constrained by the sources' own vocabulary" — is therefore correctly scoped as it stands and needs no widening.

### Added

- `component` and `work_package` columns on `vocabularies/organizational_form_characteristic.csv`, mapping each characteristic to the application's declared component framework. **WP2 / Objective 2** takes the five components as they stand in the application — entity shielding, capital lock-in, transferable claims, legal personality, perpetual succession — across 13 characteristics. **WP1 / Objective 1** is given a parallel four-component set *proposed here and not yet in the application*: owner shielding (`AP3`,`LR1`), outcome coupling (`LR2`,`LR6`), loss sharing (`LR3`,`CF2`), risk pooling (`LR4`,`LR5`). Eleven characteristics are `none` in both — description carrying no theoretical load. **Why WP1 needed its own set.** Mapping the five alone left 19 of 32 characteristics unassigned, including the whole liability-risk and contractual-form facets — that is, every result the matrix has produced. `AP3` owner shielding mapped to nothing, because the five define *what makes a corporation* (Hansmann–Kraakman–Squire) while the project's mechanism is *whether an agent's trajectory can reach zero*. Those come apart exactly at owner shielding: entity shielding protects the fund, owner shielding protects the member's path. The omission of owner shielding from the five is deliberate and defensible for the corporation definition (limited liability was late and plural, per Harris 2020) but leaves the ergodicity mechanism with no coded expression. Two component sets, one per work package, resolves it without disturbing the application's five. **The unifying formulation:** the two sets are one mechanism applied to two units. Owner shielding removes the absorbing barrier from the individual's trajectory; entity shielding, capital lock-in and perpetual succession remove it from the entity's. This predicts a trade-off — devices that protect the entity can trap the member — and `CI3` transferable claims is the hinge that resolves it, letting the member exit by sale where lock-in forbids withdrawal. `waqf_khayri` has maximal entity continuity with `CI3=0`; `bazacle_mill` has entity continuity *with* `CI3=1` from the twelfth century.
- `bazacle_mill` coded across all 32 characteristics (`OF-0124`–`OF-0155`), from Le Bris, Goetzmann & Pouget (NBER WP 31821) and Sicard (1953/2015). Coded specifically to convert the Harris versus Le Bris/Goetzmann/Pouget dispute from duelling prose lists into matrix facts. Three cells carry the argument: **`CI3=1`** — pariers could alienate without the consent of the others, and the 1182 Castel enfeoffment already schedules transaction tax on halves, thirds and quarters, so Harris's "shares were not traded in impersonal stock exchanges" concedes the structural question and objects to the venue; **`CI2=P`** — the mills invert the joint-stock pattern, accumulating capital by call with forfeiture rather than by locked-in subscription, which is a real structural difference and the strongest thing in Harris's favour; **`FP1=mixed`** — deliberately unresolved, because Harris reads community provision and the others read profit-seeking, and this single cell carries much of the disagreement. `LP1=1` records personality as *inherited* from `universitas` without state approbation, which is where the parallelism-not-convergence argument bites.
- `asiento_averia` coded across all 32 characteristics (`OF-0092`–`OF-0123`), from the vault reading of Hierro Anibarro 2005 rather than from general knowledge. Preserves that article's own limits: `AP1` is **`.NR`, not `0`** — Hierro Anibarro never asks whether a *partícipe*'s private creditors could reach the *arca* (the word *acreedor* does not occur in the text), so coding an absence would assert more than the source supports. `CI3` and `CI4` are `P` pending Sayous (1902) on the extent of *transmisión de las participaciones*. `LR5=synchronising` is coded `low` because the vault note flags where this sits on the determinate–contingent typology as an open question.
- `scripts/check_dependence.py`: validates the dependence columns against the codings. Enforces applicability (non-zero exit), reports redundancy without enforcing.
- Three columns to `vocabularies/organizational_form_characteristic.csv` recording **character dependence**, which the matrix previously left implicit:
  - `applicability_on` — the characteristic whose value gates whether this one applies at all (`LR5` on `LR4`, `LR6` on `LR2`, `CF2` on `CF1`, `MG3` on `MG1`).
  - `dependence_group` — characteristics encoding the *same underlying fact*: `agent-loss-exposure` (`AP3`,`LR1`,`CF2`,`LR6`), `legal-personhood`, `capital-immobility`, `duration`, `interest-alienability`, `pooling`.
  - `dependence_scope` — `always` where the members always collapse, `conditional` where they collapse only within a class of forms. These are two different relations and were deliberately not merged into one column: applicability is about whether a cell exists, redundancy is about whether it adds evidence. **The motivating problem:** the *ʿisqa*/*qirāḍ* contrast appears in four cells (`AP3=0`, `LR1=unlimited-several`, `CF2=1`, `LR6=symmetric`) but is one fact — the manager is a debtor on the loan half — recorded four times. Treating those as four independent characters quadruple-counts a single datum, the character-non-independence objection any referee with phylogenetic training will raise. **`agent-loss-exposure` was asserted with scope `conditional` and coding `asiento_averia` immediately tested it.** `LR1=limited` now occurs with `LR6=symmetric` (the *partícipe*, capped in both directions) *and* with `LR6=upside-only` (the *qirāḍ* agent, holding a call); `AP3=1` splits the same way. Both separations rest on substantive values, not on `.NA`. So limited liability does not determine the direction of exposure: the group collapses within bilateral capital-labour contracts, as claimed, and demonstrably not outside them. The *ʿisqa*/*qirāḍ* contrast still rests on one degree of freedom; the group as a whole does not.

### Changed

- `organizational_forms` to v0.4.0: characteristic vocabulary gains the three dependence columns. No `data.csv` row changes meaning; the 32-characteristic set is unchanged.
- `organizational_forms` to v0.3.0: characteristic set grown from 31 to 32 with `LR6`. Consumers computing per-form completeness should note the changed denominator; `waqf_khayri` and `isqa` remain fully coded at 32/32.
- `organizational_forms` schema to v0.2.0: `source_lang` enum extended from `ja|nl|de|fr|en|es` with `he`, `arc`, `ar`, `la`, `it`. The dataset codes Islamic, Jewish and Latin-Christian forms but could not cite any of them in their own language, forcing every primary locus through an English secondary work or through `.NR` — the precise conflation of "not recorded" with "not representable" that CONTRIBUTING §4 warns against. `arc` is used broadly for Aramaic; the Babylonian Talmud is strictly Jewish Babylonian Aramaic (ISO 639-3 `tmr`), and the field description records the distinction rather than multiplying codes. Additive, so no existing row changes meaning.

### Notes for verification

- **Inconsistency surfaced by the new dependence columns, now resolved — `waqf_khayri` `LR4` recoded `P` → `0` (`OF-0019`, confidence `low` → `medium`, `source_ref` `[verify]` → Kuran 2011).** The form has no members and mutualizes nothing: the corpus absorbs risk and transfers income to an indefinite beneficiary class that contributes nothing and bears no share of another's loss. That is third-party absorption, not pooling. The decisive evidence was `LR5` being inapplicable — if the waqf pooled even partially there would be baskets whose correlation `LR5` could report, and there are none, so **the child's inapplicability is evidence about the parent rather than an anomaly beside it.** The discarded `P` was carrying Kuran's claim that waqf provision substituted for what became social insurance elsewhere: a claim about function, now recorded in `notes` rather than in a structural value.
- **Coding discipline this establishes:** `P` means the characteristic is genuinely half-present. Uncertainty belongs in `confidence`, functional analogy in `notes`. A `P` sitting beside a `low` confidence and a note arguing for `0` is almost always a `0`.
- **Consequence to watch:** with `waqf_khayri` recoded, the `pooling` group now has **zero variance** across all four coded forms (`LR4=0`, `LR5=.NA` throughout) and carries no information. This is a property of which forms are coded, not a defect in the characteristics — `asiento_averia`, general average and `ko_mujin` are genuine pooling vehicles and would break it immediately. Until one is coded, no claim should rest on the pooling facet.
- Within the `agent-loss-exposure` group the four characteristics are **perfectly collinear across all four forms coded** — no form yet breaks the pattern, consistent with their being one degree of freedom. Other groups show apparent collinearity across only two forms, which is not evidence: any two characteristics look collinear at n=2. `interest-alienability` has zero variance so far (`CI3=CI4=0` for both coded forms) and carries no information until a form with tradable interests is coded.
- All 51 new rows carry `coder=ai` and are provisional. The cells most in need of a human check are `MG4` for the *ʿisqa* (coded `religious` for consistency with `waqf_khayri`, but `private-contract` is defensible and the two carry different claims), `TS3` (coded `P` because the schema's single ternary cannot represent the loan half and deposit half separately — a limitation of the characteristic, not a partial state of the form), and `FP4` (the characteristic's "political authority" does not map cleanly onto a corporate minority community).
- `organizational_forms` dataset (v0.1.0): a morphological (Zwicky) feature-coding of cooperative and risk-pooling organizational forms against a granular characteristic set, stored long/tidy (one row per form × characteristic). Provisional and partly assistant-coded (`coder=ai`, `source_ref=[verify]` where citations are unpinned), pending human verification. Seed content: `waqf_khayri` coded across all characteristics plus illustrative cells for `joint_stock`, `natie`, and `nacion_cofradia`.
- `vocabularies/organizational_form_type.csv` and `vocabularies/organizational_form_characteristic.csv`: controlled vocabularies for the two coded fields (`type_id`, `char_id`). Referenced in prose from the schema; `type_id`/`char_id` are deliberately not yet `enum`-enforced while the form census grows.

## [0.2.0] — 2026-07-23

### Added

- `variant_of` field on `clearing_records`: links an alternate-coding row (a second normalisation kept to show rate sensitivity) to the record it varies, with a self-referencing foreign key enforcing referential integrity. Makes the one-transaction/two-codings relation (CR-0003 → CR-0002) machine-readable rather than carried only in `notes` prose.

### Fixed

- De-padded `datasets/clearing_records/data.csv`. The committed file had been saved with column-alignment whitespace (a Rainbow CSV `Align` left on), which `frictionless validate` rejects: trailing spaces broke the `record_id` pattern and every `enum` constraint, so CI was failing on the illustrative dataset. Per `EDITING-CSV.md`, the committed CSV is unpadded; alignment is a view-time toggle only.

## [0.1.0] — 2026-07-08

### Added

- Initial repository scaffold and governance layer.
- `clearing_records` illustrative dataset (synthetic) demonstrating the schema.
- Provisional schema. Provenance apparatus established from commit one.

Snapshot minted to a FigShare and Zenodo DOI on this, the first tagged release: [Zenodo](https://doi.org/10.5281/zenodo.21341361) · [FigShare](https://doi.org/10.6084/m9.figshare.32947250).
